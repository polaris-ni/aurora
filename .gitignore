# 通用规则：忽略所有以 . 开头的文件夹，.github 与 .git 除外
.*
!/.github/
!/.git/

# 构建产物
*build*/**

# 编译/测试中间产物
__pycache__/

# 测试临时文件（C4 约定：用例临时目录落在 <repo>/test_temp/，每条用例结束自动清理、会话结束清空）
test_temp/

# 规划/设计草稿：未纳入 codespec/ 的开发过程文档，只作本地推演载体，不进版本库
*.draft.md

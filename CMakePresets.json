{
  "version": 6,
  "cmakeMinimumRequired": {
    "major": 3,
    "minor": 20,
    "patch": 0
  },
  "configurePresets": [
    {
      "name": "ninja",
      "displayName": "Ninja (default, parallel)",
      "description": "Use Ninja generator for maximum parallel build throughput (default recommended).",
      "generator": "Ninja",
      "binaryDir": "${sourceDir}/build",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Release",
        "CMAKE_C_COMPILER": "gcc",
        "CMAKE_CXX_COMPILER": "g++"
      }
    },
    {
      "name": "mingw",
      "displayName": "MinGW Makefiles (legacy)",
      "description": "Keep the historical MinGW Makefiles workflow (non-parallel; use -- -j N).",
      "generator": "MinGW Makefiles",
      "binaryDir": "${sourceDir}/build",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Release",
        "CMAKE_C_COMPILER": "gcc",
        "CMAKE_CXX_COMPILER": "g++"
      }
    },
    {
      "name": "wasm",
      "displayName": "Emscripten / WebAssembly (build-wasm)",
      "description": "Equivalent to CI's `emcmake cmake` invocation: Emscripten toolchain via %EMSDK% (set by `emsdk activate --permanent`), WASM backend on, tests on (run them with `ctest --test-dir build-wasm`), demos on-demand.",
      "generator": "Ninja",
      "binaryDir": "${sourceDir}/build-wasm",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Release",
        "CMAKE_TOOLCHAIN_FILE": "$env{EMSDK}/upstream/emscripten/cmake/Modules/Platform/Emscripten.cmake",
        "AURORA_BACKEND_WASM": "ON",
        "AURORA_BUILD_TESTS": "ON",
        "AURORA_BUILD_DEMOS": "ON"
      }
    }
  ],
  "buildPresets": [
    {
      "name": "wasm",
      "displayName": "wasm library (aurora)",
      "configurePreset": "wasm",
      "targets": ["aurora"]
    }
  ],
  "testPresets": [
    {
      "name": "ninja-test",
      "displayName": "Test (build/, parallel)",
      "description": "Run the full ctest suite in build/ with per-test process isolation in parallel (equivalent to ctest --test-dir build --output-on-failure -j N).",
      "configurePreset": "ninja",
      "output": {
        "outputOnFailure": true
      },
      "execution": {
        "jobs": 16
      }
    }
  ]
}

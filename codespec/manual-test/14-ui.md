# ui 模块人工测试用例

> **文档状态：临时**
> 本文件按固定格式编写，供程序解析：字段顺序、字段名、值域与分隔符均为约定的一部分，见 §1.5。
> 格式规范与首份示例见 `codespec/manual-test/01-core.md`。

## 1 适用范围与约定

### 1.1 被测模块

`ui/` —— 声明式工厂层（1 个公共头 `factories.h`），在实测依赖分层中位于 L5。

该模块只做一件事：把「构造控件 → 设置属性 → 加入容器」三步合并为一次函数调用，同时自动登记属性监听器。其价值不在功能覆盖，而在**写法一致的构造入口**——是本库「AI 友好、代码可生成」定位的直接体现。

### 1.2 执行载体

复用既有示例程序，不新增载体：

| 载体 | 构建目标 | 用途 |
|:---|:---|:---|
| `examples/demos/demo_ui_factory.cpp` | `demo_ui_factory` | 工厂调用产生的控件树及其富格式文本 dump |

该载体的观测方式与其余图形 demo 不同：它在构建完控件树后，把 `au::dump_tree_rich` 的输出经 `AURORA_LOG_RAW` 写到 **stdout**，因此无需人眼读取窗口内容即可获得完整树结构。这使「工厂调用次数是否与生成的节点数一致」成为可直接判定的事实。

### 1.3 执行环境

| 项目 | 要求 |
|:---|:---|
| 平台 | Windows 本机（实测环境为 MinGW/GCC + Ninja） |
| 构建 | 库已构建；载体为按需目标，不在默认构建内 |
| 终端 | 支持 UTF-8 输出（树 dump 含制表符与 `├─`、`└─` 等框线字符） |
| 分离输出 | **须分离重定向**：树 dump 在 stdout、诊断日志在 stderr |

### 1.4 用例编号规则

格式 `TC-<模块段>-<三位序号>`，自 001 起连续编号。
模块段为**模块目录名的大写形式**（`core` → `CORE`、`i18n` → `I18N`），**不得自创缩写**——目录名含数字时数字原样保留，故正则为 `^TC-[A-Z0-9]+-\d{3}$`。
本模块目录名 `ui`，即 `TC-UI-001` 起。序号在本模块内唯一且**不复用**；用例被删除后其编号作废，新用例取下一个可用序号。
用例在文档中**按编号升序声明**——该顺序即默认执行顺序，也是依赖列表排序的依据（见 §1.5）。

### 1.5 用例字段清单与格式规范

每条用例由**六个固定字段**组成，排布于同一张 Markdown 表格内。**字段顺序固定、字段名一字不差、每字段独占一行、单元格内不换行**（需要多条内容时用 `<br>` 分隔）：

| 序号 | 字段 | 格式 | 正则约束 | 说明 |
|:--:|:---|:---|:---|:---|
| 1 | 用例编号 | 单行 | `^TC-[A-Z0-9]+-\d{3}$` | 模块内唯一且不复用 |
| 2 | 测试目的 | 单行 | 自由文本 | 一句话，指向**单一**验证目标 |
| 3 | 前置条件 | 单行 | 自由文本 | 只描述**环境状态**；「必须先执行某用例」属依赖用例字段，不在此重复 |
| 4 | 依赖用例 | 单行 | `^无$` 或 `^TC-[A-Z0-9]+-\d{3}(, TC-[A-Z0-9]+-\d{3})*$` | 见下方规范 |
| 5 | 操作步骤 | 条目列表 | 每条目以 `^\d+\. ` 开头，条目间以 `<br>` 分隔 | 自 1 起连续编号 |
| 6 | 预期结果 | 条目列表 | 同操作步骤；编号是步骤号的**子集** | 见 §1.6 |

**依赖用例字段规范（四条硬性要求）：**

1. 无依赖时**必须**写 `无`，不得留空、不得写 `-`、`N/A`、`none` 等变体。
2. 有依赖时写**用例编号全称**（含模块段），不写省略形式；跨模块依赖同样写全称。
3. 多项依赖以**半角逗号加一个空格**（`, `）分隔；逗号前后不得出现空格以外的字符。
4. 多项依赖的**排列顺序即依赖先后**——先决者在前。因用例按编号升序声明，等价于按被依赖用例的**编号升序**排列。

**模块级约定**：本模块载体的构建步骤统一置于 `TC-UI-001` 的步骤 1；其余用例不在步骤中重复构建，而是通过依赖用例字段间接依赖其产出的已构建载体。

**为什么必须统一格式**：本文件后续将由程序解析（生成依赖拓扑、按步骤号统计失败点、汇总通过率）。上述约定使每个字段可用**单条正则**校验，无需自然语言推断。

### 1.6 操作步骤与预期结果编号规则

**预期结果与操作步骤同号一一对应**：`预期结果 N` 即「步骤 N 的对应结果」。

1. 预期结果的序号**等于**其对应步骤的序号，两者严格相等——不得因省略而重排、压缩或重新连续编号。
2. 纯执行步骤**不产生预期结果**，其序号在预期结果列表中直接跳过。
3. 故预期结果的序号**不要求连续**，条数也无需等于步骤数。
4. 步骤列表中，纯执行步骤在文本末尾标注「（纯执行，无预期结果）」，供与预期结果列表逐号对照。

示例：某用例有步骤 1、2、3、4，其中步骤 3 为纯执行步骤，则该用例的预期结果共 3 条，编号为 1、2、4。

**该规则解决的问题**：执行者按步骤顺序推进时，编号本身即是映射关系，无需自行推断「这条预期结果对应哪一步」；执行记录可借步骤号精确落到失败点。

### 1.7 判定与记录

| 结果 | 含义 |
|:---|:---|
| PASS | 全部非空预期结果均满足 |
| FAIL | 任一非空预期结果不满足；须在记录表登记该预期结果对应的**步骤号**、实际现象与缺陷编号 |
| BLOCKED | 前置条件无法满足（如载体构建失败），未开始执行 |
| SKIP | 主动判定不适用于本环境（如被测平台与用例限定平台不符） |

输出中含**时间戳、线程 id、行号**的字段属可变部分，判定时只核对格式与是否出现，不比对具体值。

**执行记录表格式**（列顺序固定）：

| 列 | 格式 | 允许值 / 正则 |
|:---|:---|:---|
| 用例编号 | 单行 | 与 §2 用例编号一致 |
| 执行日期 | 单行 | `^\d{4}-\d{2}-\d{2}$` |
| 执行人 | 单行 | 自由文本 |
| 结果 | 单行 | `PASS` / `FAIL` / `BLOCKED` / `SKIP` |
| 失败步骤号 | 单行 | 留空（未失败或未执行），或 `^\d+(, \d+)*$`（多个以半角逗号加一个空格分隔） |
| 实际现象 | 单行 | 自由文本 |
| 缺陷编号 | 单行 | 留空或缺陷单号 |
| 备注 | 单行 | 自由文本 |

未执行的取值一律**留空单元格**，不写 `-`、`TBD`、`N/A` 等占位符。

### 1.8 覆盖取舍

本模块只有 1 个头，且其函数均为「构造 + 追加」的薄封装，**单个工厂函数的正确性已由自动化覆盖**（`tests/unit/utest_factories.cpp`）。

人工用例只针对工厂层特有的、程序不便断言的性质：

| 子域 | 人工判定优于程序判定的原因 |
|:---|:---|
| 调用与产出的对应关系 | 「每次工厂调用恰好产生一个子节点」是跨函数的不变量，需对照调用序列与树 dump 逐项核对 |
| 自动登记监听器 | 工厂在构造时是否替调用者挂上了合理的事件钩子，需读出监听器清单判断 |
| 树 dump 的可用性 | dump 是否含定位所需的字段（id、几何、可见性、文本、样式、监听器）——这直接决定 AI 与调试者能否凭文本定位控件，属可用性判断，不是正确性判断 |

因此本模块**不设「不纳入」表**：唯一的头已整体列入上述三个子域，其余函数（`label`、`button`、`input`、`checkbox`、`slider`）的行为由上述用例间接覆盖，其精确参数校验归 `utest_factories`。

## 2 用例清单

### 2.1 载体基线与树 dump

#### TC-UI-001 载体构建与启动基线及树 dump 输出

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-UI-001 |
| 测试目的 | 确认载体可构建运行，且富格式树 dump 走 stdout 而诊断日志走 stderr，两条通道互不污染 |
| 前置条件 | 位于仓库根目录；`build/` 已完成 CMake 配置；本机工具链可用 |
| 依赖用例 | 无 |
| 操作步骤 | 1. 构建载体：`cmake --build build --target demo_ui_factory`（纯执行，无预期结果）<br>2. 启动 `./build/demo_ui_factory.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>3. 查看窗口标题与客户区尺寸<br>4. 查看 `out.txt` 的形态与行数<br>5. 查看 `err.txt` 的内容与行数 |
| 预期结果 | 3. 标题为 `UI Factory · Aurora Demo`，客户区约为 480×420<br>4. `out.txt` 为**富格式树文本**（非 JSON）：以框线字符 `└─` 与 `├─` 表达层级、以缩进表达深度，共 6 行（1 个根节点 + 5 个直接子节点）；不含时间戳或级别标签<br>5. 恰 1 行，级别为 `INF`，消息含 `[run_demo] window shown: UI Factory · Aurora Demo`（树 dump 未混入 stderr） |

#### TC-UI-002 工厂调用次数与生成节点数一致

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-UI-002 |
| 测试目的 | 验证每次工厂调用恰好向根容器追加一个子节点，不多不少（工厂层最易出的错是重复追加或漏追加） |
| 前置条件 | 载体 `demo_ui_factory` 已构建成功；可读源码中的工厂调用序列 |
| 依赖用例 | TC-UI-001 |
| 操作步骤 | 1. 启动 `./build/demo_ui_factory.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>2. 在源码中清点对 `au::ui::` 工厂函数的调用次数（纯执行，无预期结果）<br>3. 在 `out.txt` 中清点根节点下的直接子节点行数（纯执行，无预期结果）<br>4. 对照步骤 2 与步骤 3 的数量<br>5. 按源码中的调用顺序核对子节点类型序列 |
| 预期结果 | 4. 两者**相等**（源码共 6 次工厂调用：两次 `label`、一次 `button`、一次 `input`、一次 `checkbox`、一次 `slider`；`out.txt` 的根节点下恰 6 个直接子节点）<br>5. 子节点类型自上而下依次为 `Text`、`Text`、`Button`、`TextInput`、`Checkbox`、`Slider`，与源码调用顺序一致 |

#### TC-UI-003 树 dump 含定位所需的六项信息

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-UI-003 |
| 测试目的 | 验证树 dump 的信息密度足以支撑「凭文本定位控件」，这是 AI 工具链与调试流程的前提 |
| 前置条件 | 载体 `demo_ui_factory` 已构建成功 |
| 依赖用例 | TC-UI-001 |
| 操作步骤 | 1. 启动 `./build/demo_ui_factory.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>2. 检查根节点行是否带 id<br>3. 检查每个节点行是否含几何信息<br>4. 检查每个节点行是否含可见性字段<br>5. 检查文本类节点是否显示其文本内容<br>6. 检查每个节点行是否含样式与监听器信息 |
| 预期结果 | 2. 根节点行形如 `Column#demo-root`，即带 `#` 前缀的 id（源码中经 `set_id("demo-root")` 设置）<br>3. 每个节点行均含 `bounds:` 字段，格式为四个数值（原点与尺寸），即使为全零也照样输出<br>4. 每个节点行均含 `visible:` 字段，取值 `true` / `false`<br>5. `Text` 节点的 `text:` 字段显示其真实文本（如 `Aurora UI factory layer demo`）；非文本节点显示空串<br>6. 每个节点行均含 `style:` 与 `listeners:` 两个字段；`listeners:` 的取值可区分有无 |

#### TC-UI-004 工厂为控件自动登记对应监听器

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-UI-004 |
| 测试目的 | 验证工厂构造的控件在默认情况下已挂上该类控件应有的事件监听器，非空壳 |
| 前置条件 | 载体 `demo_ui_factory` 已构建成功 |
| 依赖用例 | TC-UI-001, TC-UI-003 |
| 操作步骤 | 1. 启动 `./build/demo_ui_factory.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>2. 在 `out.txt` 中找到 `Button` 节点行，读取其 `listeners:` 字段<br>3. 找到 `TextInput` 节点行，读取其 `listeners:` 字段<br>4. 找到 `Checkbox` 与 `Slider` 节点行，读取其 `listeners:` 字段<br>5. 对照各控件类型应有的事件（按钮应可点击、输入框应可提交与变更、复选框与滑块应可变更） |
| 预期结果 | 2. `Button` 含 `on_click`<br>3. `TextInput` 含 `on_focus_change`、`on_changed`、`on_submit` 三类<br>4. `Checkbox` 与 `Slider` 均含 `on_changed`<br>5. 各控件登记的监听器与其交互语义相符，不存在「某控件完全没有监听器」的情形（若工厂构造的控件为空壳，交互将全部失效） |

## 3 执行记录表

| 用例编号 | 执行日期 | 执行人 | 结果 | 失败步骤号 | 实际现象 | 缺陷编号 | 备注 |
|:---|:---|:---|:---|:---|:---|:---|:---|
| TC-UI-001 | | | | | | | |
| TC-UI-002 | | | | | | | |
| TC-UI-003 | | | | | | | |
| TC-UI-004 | | | | | | | |

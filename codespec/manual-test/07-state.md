# state 模块人工测试用例

> **文档状态：临时**
> 本文件按固定格式编写，供程序解析：字段顺序、字段名、值域与分隔符均为约定的一部分，见 §1.5。
> 格式规范与首份示例见 `codespec/manual-test/01-core.md`。

## 1 适用范围与约定

### 1.1 被测模块

`state/` —— 响应式状态层（14 个公共头），在实测依赖分层中位于 L3。

实测分层结论：`layout`、`render`、`state` 三者**互不依赖、平级**（不要按 `codespec/ARCHITECTURE.md` §2 层图的 `layout → render → state` 链式顺序理解）。

本模块的可观测行为集中在两类：

| 类别 | 内容 |
|:---|:---|
| 属性与状态的绑定路径 | 属性包装 `Reactive<T>` 由「值」构造与由「共享 `State`」构造，两者的刷新行为不同（见 §2.2） |
| 刷新落点与观感 | 状态写回后刷新是否只落在受影响控件上，以及异步写回在窗口上的呈现 |

### 1.2 执行载体

复用既有示例程序，不新增载体：

| 载体 | 构建目标 | 用途 |
|:---|:---|:---|
| `examples/demos/demo_state.cpp` | `demo_state` | `State` / `Computed` / `Binding` / `Store` 与属性的绑定路径 |
| `examples/demos/demo_async.cpp` | `demo_async` | 异步写回触发的属性刷新、协程续体 |
| `examples/demos/demo_provider.cpp` | `demo_provider` | 三层 Provider 嵌套（主题 / 语言 / 媒体查询） |

三个载体均为窗口程序，观测方式为「运行后操作窗口并读取呈现」，判定由人工完成。

### 1.3 执行环境

| 项目 | 要求 |
|:---|:---|
| 平台 | Windows 本机（实测环境为 MinGW/GCC + Ninja） |
| 构建 | 库已构建；三个载体为按需目标，不在默认构建内 |
| 终端 | 支持 UTF-8 输出 |
| 分离输出 | 诊断日志写 stderr；判定 stderr 时需先重定向 |
| 交互 | 需真实鼠标点击（`demo_state` 的两个按钮） |

`demo_async` 的状态写回依赖后台线程完成一次约 2 秒的模拟计算，观测时间窗与本地读取速度相关，见 §2.3 的判定说明。

### 1.4 用例编号规则

格式 `TC-<模块段>-<三位序号>`，自 001 起连续编号。
模块段为**模块目录名的大写形式**（`core` → `CORE`、`i18n` → `I18N`；取 `include/aurora/` 下的模块目录名，本目录文件名前缀 `NN-` 是文档序号、不计入模块段，如 `01-core.md` → `CORE`），**不得自创缩写**——目录名含数字时数字原样保留，故正则为 `^TC-[A-Z0-9]+-\d{3}$`。
本模块目录名 `state`，即 `TC-STATE-001` 起。序号在本模块内唯一且**不复用**；用例被删除后其编号作废，新用例取下一个可用序号。
用例在文档中**按编号升序声明**——该顺序即默认执行顺序，也是依赖列表排序的依据（见 §1.5）。

### 1.5 用例字段清单与格式规范

每条用例由**六个固定字段**组成，排布于同一张 Markdown 表格内。**字段顺序固定、字段名一字不差、每字段独占一行、单元格内不换行**（需要多条内容时用 `<br>` 分隔）：

| 序号 | 字段 | 格式 | 正则约束 | 说明 |
|:--:|:---|:---|:---|:---|
| 1 | 用例编号 | 单行 | `^TC-[A-Z0-9]+-\d{3}$` | 模块内唯一且不复用 |
| 2 | 测试目的 | 单行 | 自由文本 | 一句话，指向**单一**验证目标 |
| 3 | 前置条件 | 单行 | 自由文本 | 只描述**环境状态**；「必须先执行某用例」属依赖用例字段，不在此重复 |
| 4 | 依赖用例 | 单行 | `^无$` 或 `^TC-[A-Z0-9]+-\d{3}(, TC-[A-Z0-9]+-\d{3})*$` | 见下方规范 |
| 5 | 操作步骤 | 条目列表 | 每条目以 `^\d+\. ` 开头，条目间以 `<br>` 分隔 | 自 1 起连续编号 |
| 6 | 预期结果 | 条目列表 | 同操作步骤；编号是步骤号的**子集** | 见 §1.6 |

**依赖用例字段规范（四条硬性要求）：**

1. 无依赖时**必须**写 `无`，不得留空、不得写 `-`、`N/A`、`none` 等变体。
2. 有依赖时写**用例编号全称**（含模块段），不写省略形式；跨模块依赖同样写全称。
3. 多项依赖以**半角逗号加一个空格**（`, `）分隔；逗号前后不得出现空格以外的字符。
4. 多项依赖的**排列顺序即依赖先后**——先决者在前。因用例按编号升序声明，等价于按被依赖用例的**编号升序**排列。

**模块级约定**：本模块三个载体的构建步骤统一置于 `TC-STATE-001` 的步骤 1（该步骤一次构建全部三个目标）；其余用例不在步骤中重复构建，而是通过依赖用例字段间接依赖其产出的已构建载体。

**为什么必须统一格式**：本文件由 `tools/check/check_manual_test_format.py` 解析守护（CTest 用例 `check_manual_test_format`，校验字段名与顺序、取值域、依赖拓扑、步骤-预期同号映射与执行记录表格式）。上述约定使每个字段可用**单条正则**校验，无需自然语言推断。

### 1.6 操作步骤与预期结果编号规则

**预期结果与操作步骤同号一一对应**：`预期结果 N` 即「步骤 N 的对应结果」。

1. 预期结果的序号**等于**其对应步骤的序号，两者严格相等——不得因省略而重排、压缩或重新连续编号。
2. 纯执行步骤**不产生预期结果**，其序号在预期结果列表中直接跳过。
3. 故预期结果的序号**不要求连续**，条数也无需等于步骤数。
4. 步骤列表中，纯执行步骤在文本末尾标注「（纯执行，无预期结果）」，供与预期结果列表逐号对照。

示例：某用例有步骤 1、2、3、4，其中步骤 3 为纯执行步骤，则该用例的预期结果共 3 条，编号为 1、2、4。

**该规则解决的问题**：执行者按步骤顺序推进时，编号本身即是映射关系，无需自行推断「这条预期结果对应哪一步」；执行记录可借步骤号精确落到失败点。

### 1.7 判定与记录

| 结果 | 含义 |
|:---|:---|
| PASS | 全部非空预期结果均满足 |
| FAIL | 任一非空预期结果不满足；须在记录表登记该预期结果对应的**步骤号**、实际现象与缺陷编号 |
| BLOCKED | 前置条件无法满足（如载体构建失败），未开始执行 |
| SKIP | 主动判定不适用于本环境（如被测平台与用例限定平台不符） |

输出中含**时间戳、线程 id、行号**的字段属可变部分，判定时只核对格式与是否出现，不比对具体值。

**执行记录表格式**（列顺序固定）：

| 列 | 格式 | 允许值 / 正则 |
|:---|:---|:---|
| 用例编号 | 单行 | 与 §2 用例编号一致 |
| 执行日期 | 单行 | `^\d{4}-\d{2}-\d{2}$` |
| 执行人 | 单行 | 自由文本 |
| 结果 | 单行 | `PASS` / `FAIL` / `BLOCKED` / `SKIP` |
| 失败步骤号 | 单行 | 留空（未失败或未执行），或 `^\d+(, \d+)*$`（多个以半角逗号加一个空格分隔） |
| 实际现象 | 单行 | 自由文本 |
| 缺陷编号 | 单行 | 留空或缺陷单号 |
| 备注 | 单行 | 自由文本 |

未执行的取值一律**留空单元格**，不写 `-`、`TBD`、`N/A` 等占位符。

### 1.8 覆盖取舍

本模块按「人工判定是否优于程序判定」取舍，不追求对 14 个头的全覆盖。

**纳入人工测试的子域**：

| 子域 | 人工判定优于程序判定的原因 |
|:---|:---|
| 属性包装的构造路径 | 同一份「看似响应式」的代码，因属性由值构造还是由共享状态构造，**行为不同**；该差异在类型层面不可见（两者都是 `Reactive<T>`），只能运行后观察 |
| 异步写回的刷新落点 | 「只刷新受影响控件」是**观感不变量**：程序能断言回调被调用，但无法断言屏幕没有整体重绘或闪烁 |
| 进程生命周期 | 关闭窗口后进程能否干净退出，需真实窗口环境 |
| Provider 嵌套 | 三层注入是否破坏子树渲染，属端到端观感 |

**不纳入人工测试的头及其理由**：

| 头 | 理由 | 已有覆盖 |
|:---|:---|:---|
| `state.h` `signal_view.h` | 读写语义与依赖登记，输入输出可穷举断言 | `utest_state` |
| `effect.h` | 依赖收集范围与重跑时机，程序判定更精确 | `utest_effect` |
| `computed.h` | 派生值求解与惰性求值，无视觉表现 | `utest_computed` |
| `store.h` | reducer 纯函数与派发语义 | `utest_store` |
| `subscription.h` `binding.h` | 订阅生命周期与取消幂等性 | `utest_subscription` `utest_binding` |
| `state_graph.h` `state_registry.h` | 依赖图输出结构 | `utest_state_graph` `utest_state_registry` |
| `immutable.h` | 不可变容器结构与共享语义 | `utest_immutable` |
| `async.h` `coroutine.h` | 调度时序不确定，人工观察不可复现；仅其**观感与刷新落点**纳入（TC-STATE-003、TC-STATE-004、TC-STATE-007） | `utest_async` `utest_coroutine` |
| `undo_stack.h` | 无对应载体，且语义为纯游标逻辑（入栈、撤销、重做、分支截断、深度上限），无可观测的视觉或体感表现 | `utest_undo_stack` |
| `reactive.h` | 类型定义，其**两条构造路径的行为差异**已由 TC-STATE-002 与 TC-STATE-003 覆盖 | — |

## 2 用例清单

### 2.1 载体基线与生命周期

#### TC-STATE-001 载体构建与启动基线

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-STATE-001 |
| 测试目的 | 确认本模块三个载体均可构建、启动并正常显示窗口，为后续用例提供基线 |
| 前置条件 | 位于仓库根目录；`build/` 已完成 CMake 配置；本机工具链可用 |
| 依赖用例 | 无 |
| 操作步骤 | 1. 构建载体：`cmake --build build --target demo_state demo_async demo_provider`（纯执行，无预期结果）<br>2. 启动 `./build/demo_state.exe` 并以 `1> out.txt 2> err.txt` 分离重定向（纯执行，无预期结果）<br>3. 查看窗口标题栏文本<br>4. 查看 `err.txt` 的内容与行数<br>5. 关闭窗口并记录进程退出码<br>6. 以同法启动 `./build/demo_provider.exe` 并查看其窗口标题与 `err.txt` |
| 预期结果 | 3. 标题为 `State · Aurora Demo`<br>4. 恰 1 行，级别为 `INF`，消息含 `[run_demo] window shown: State · Aurora Demo`<br>5. 退出码为 0，且 `err.txt` 中无 `ERR` 或 `FTL` 级日志<br>6. `demo_provider` 的标题为 `Provider · Aurora Demo`，`err.txt` 同为恰 1 行 `INF` 级 `[run_demo] window shown: Provider · Aurora Demo` |

### 2.2 属性包装的构造路径

#### TC-STATE-002 由值构造的属性持有静态快照，不随源状态更新

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-STATE-002 |
| 测试目的 | 验证属性由**值**构造时只取构造瞬间的快照：后续状态变更不会驱动该属性刷新 |
| 前置条件 | 载体 `demo_state` 已构建成功；鼠标可用 |
| 依赖用例 | TC-STATE-001 |
| 操作步骤 | 1. 启动 `./build/demo_state.exe`（纯执行，无预期结果）<br>2. 读取窗口内 `count =` 起始文本<br>3. 点击 `State +1` 按钮一次，再次读取该文本<br>4. 继续点击 `State +1` 直至累计 5 次，再次读取该文本<br>5. 读取 `Store size =` 文本，点击 `Store add` 按钮一次后再次读取 |
| 预期结果 | 2. 显示 `count = 3`（与 `State<int>` 初值 3 一致）<br>3. 仍为 `count = 3`，文本未变<br>4. 仍为 `count = 3`，文本未变<br>5. `Store size =` 在点击前后均为 `Store size = 2`。<br>**判定要点**：不变是**预期行为**，不是缺陷——属性类型 `Reactive<LocalizedString>` 接收的是由 `LocalizedString{status.get()}` 隐式构造出的独立 `State`，与外部 `Computed` 之间没有共享源；需要随状态刷新时应改用共享状态构造（对照 TC-STATE-003） |

#### TC-STATE-003 由共享状态构造的属性随异步写回更新

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-STATE-003 |
| 测试目的 | 验证属性由**共享状态**构造时订阅生效：后台线程写回状态后控件文本随之更新 |
| 前置条件 | 载体 `demo_async` 已构建成功；当前目录可写 |
| 依赖用例 | TC-STATE-001 |
| 操作步骤 | 1. 启动 `./build/demo_async.exe` 并以 `1> out.txt 2> err.txt` 分离重定向（纯执行，无预期结果）<br>2. 在窗口刚出现的瞬间读取状态行文本，记录其内容<br>3. 等待 5 秒后再次读取该行文本<br>4. 查看 `err.txt` 的全部内容 |
| 预期结果 | 2. 若读到 `running…` 说明捕获到了初始态；若读到 `result = done` 说明本地读取耗时超过了约 2 秒的模拟计算时长，**此时不判 FAIL**，以步骤 3 为准<br>3. 显示 `result = done`——该值与后台任务返回值一致，证明状态行确实随状态写回刷新（与 TC-STATE-002 的静态快照形成对照）<br>4. 含 `[INF]` 级的 `[run_demo] window shown: Async · Aurora Demo`；窗口显示的 5 秒内无 `ERR` 或 `FTL` 级日志 |

### 2.3 协程续体

#### TC-STATE-004 协程在后台求值并经 Result 回传

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-STATE-004 |
| 测试目的 | 验证 `co_await` 提交的后台计算完成后，续体在主线程侧取得结果并可按成功路径处理 |
| 前置条件 | 载体 `demo_async` 已构建成功；当前目录可写 |
| 依赖用例 | TC-STATE-001 |
| 操作步骤 | 1. 从仓库根启动 `./build/demo_async.exe` 并以 `2> err.txt` 重定向 stderr（纯执行，无预期结果）<br>2. 在 `err.txt` 中定位分类为 `coro` 的行（纯执行，无预期结果）<br>3. 查看该行的级别、消息内容与位置字段<br>4. 关闭窗口，确认进程退出码 |
| 预期结果 | 3. 恰 1 行，级别为 `INF`，消息为 `computed = 42`（与 `21 * 2` 一致，说明走的是成功分支而非失败分支）；位置字段指向 `examples/demos/demo_async.cpp`<br>4. 退出码为 0 |

### 2.4 刷新落点与 Provider 嵌套

#### TC-STATE-005 状态写回只刷新受影响控件

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-STATE-005 |
| 测试目的 | 验证状态变更触发的重绘落在受影响控件上，而非整窗重绘（整窗重绘在视觉上表现为闪烁或残影） |
| 前置条件 | 载体 `demo_async` 已构建成功；显示器可清晰观察窗口全貌 |
| 依赖用例 | TC-STATE-001, TC-STATE-003 |
| 操作步骤 | 1. 启动 `./build/demo_async.exe`（纯执行，无预期结果）<br>2. 注视窗口整体，等待状态行由 `running…` 变为 `result = done`（纯执行，无预期结果）<br>3. 观察文本变化瞬间窗口其余区域的表现 |
| 预期结果 | 3. 仅状态行所在的文本区域发生变化；标题渐变区、两行说明文字、卡片边框与背景在该瞬间**均无闪烁、无白闪、无重影**。<br>**判定要点**：若观察到整窗短暂变白或元素重影，说明刷新落点超出受影响控件，即 FAIL |

#### TC-STATE-006 三层 Provider 嵌套子树渲染完整

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-STATE-006 |
| 测试目的 | 验证主题 / 语言 / 媒体查询三层注入嵌套后，被包裹子树仍完整构建并渲染，无内容缺失 |
| 前置条件 | 载体 `demo_provider` 已构建成功 |
| 依赖用例 | TC-STATE-001 |
| 操作步骤 | 1. 启动 `./build/demo_provider.exe`（纯执行，无预期结果）<br>2. 检查窗口内容区是否按 `Column` 顺序呈现全部四项内容<br>3. 读取其中的 scale 说明行<br>4. 检查 stderr 是否有 `ERR` 或 `FTL` 级日志 |
| 预期结果 | 2. 自上而下依次呈现标题、Provider 说明行、品牌徽标、scale 说明行共四项，无空白占位或缺项<br>3. 显示 `scale = 2.0 (from MediaQuery)`<br>4. 无 `ERR` / `FTL` 级日志 |

### 2.5 窗口关闭路径

#### TC-STATE-007 关闭窗口后进程干净退出

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-STATE-007 |
| 测试目的 | 验证状态与观察者尚未释放时关闭窗口，进程仍能正常收尾退出（无悬挂线程、无崩溃） |
| 前置条件 | 载体 `demo_async` 已构建成功（该载体的后台任务与协程使其持有更长的生命周期引用） |
| 依赖用例 | TC-STATE-001 |
| 操作步骤 | 1. 启动 `./build/demo_async.exe` 并以 `2> err.txt` 重定向 stderr（纯执行，无预期结果）<br>2. 在状态行已变为 `result = done` 后，点击窗口关闭按钮（纯执行，无预期结果）<br>3. 记录进程退出码并查看 `err.txt` 末尾<br>4. 以同法在 `demo_state` 上重复一次 |
| 预期结果 | 3. 退出码为 0；`err.txt` 末尾无崩溃或异常相关输出（无 `ERR` / `FTL` 级日志）<br>4. `demo_state` 的退出码同为 0，`err.txt` 末尾同样干净 |

## 3 执行记录表

| 用例编号 | 执行日期 | 执行人 | 结果 | 失败步骤号 | 实际现象 | 缺陷编号 | 备注 |
|:---|:---|:---|:---|:---|:---|:---|:---|
| TC-STATE-001 | | | | | | | |
| TC-STATE-002 | | | | | | | |
| TC-STATE-003 | | | | | | | |
| TC-STATE-004 | | | | | | | |
| TC-STATE-005 | | | | | | | |
| TC-STATE-006 | | | | | | | |
| TC-STATE-007 | | | | | | | |

# event 模块人工测试用例

> **文档状态：临时**
> 本文件按固定格式编写，供程序解析：字段顺序、字段名、值域与分隔符均为约定的一部分，见 §1.5。
> 格式规范与首份示例见 `codespec/manual-test/01-core.md`。

## 1 适用范围与约定

### 1.1 被测模块

`event/` —— 输入事件层（6 个公共头），在实测依赖分层中位于 L4。

| 头 | 职责 |
|:---|:---|
| `event.h` | 事件类型定义（鼠标 / 键盘 / 触摸 / 文本输入） |
| `dispatcher.h` | 事件分发：命中测试与冒泡 |
| `focus.h` | 焦点管理、Tab 遍历、焦点作用域（焦点陷阱） |
| `gesture.h` | 手势识别（拖拽 / 双指缩放 / 旋转） |
| `drag_drop.h` | 拖放 |
| `keycode.h` | 键码映射 |

本模块的**人工增益极高**：真实输入设备的行为（按下后移出控件再松开、拖拽跨越控件边界、键盘焦点遍历）无法由程序构造出等价场景——单元测试能喂入合成事件对象，但无法复现「指针被系统捕获后仍把移动事件送回原控件」这类真实链路。

### 1.2 执行载体

复用既有示例程序，不新增载体：

| 载体 | 构建目标 | 用途 |
|:---|:---|:---|
| `examples/demos/demo_event.cpp` | `demo_event` | 点击命中与派发、高频点击 |
| `examples/demos/demo_multitouch.cpp` | `demo_multitouch` | 拖拽路由、指针捕获、多点触控原始流、Tab 焦点遍历 |

两个载体均将观测结果写入 stderr（分类均为 `demo`），因此**判定依赖 stderr 行数**，运行前须重定向。

### 1.3 执行环境

| 项目 | 要求 |
|:---|:---|
| 平台 | Windows 本机（实测环境为 MinGW/GCC + Ninja） |
| 构建 | 库已构建；两个载体为按需目标，不在默认构建内 |
| 终端 | 支持 UTF-8 输出 |
| 分离输出 | 诊断日志写 stderr；判定前须重定向 |
| 指针 | 真实鼠标（触摸板亦可，但拖拽类用例建议用鼠标） |
| 触摸屏 | **仅 TC-EVENT-005 需要**；无多点触控硬件时该用例记 `SKIP` |

### 1.4 用例编号规则

格式 `TC-<模块段>-<三位序号>`，自 001 起连续编号。
模块段为**模块目录名的大写形式**（`core` → `CORE`、`i18n` → `I18N`；取 `include/aurora/` 下的模块目录名，本目录文件名前缀 `NN-` 是文档序号、不计入模块段，如 `01-core.md` → `CORE`），**不得自创缩写**——目录名含数字时数字原样保留，故正则为 `^TC-[A-Z0-9]+-\d{3}$`。
本模块目录名 `event`，即 `TC-EVENT-001` 起。序号在本模块内唯一且**不复用**；用例被删除后其编号作废，新用例取下一个可用序号。
用例在文档中**按编号升序声明**——该顺序即默认执行顺序，也是依赖列表排序的依据（见 §1.5）。

### 1.5 用例字段清单与格式规范

每条用例由**六个固定字段**组成，排布于同一张 Markdown 表格内。**字段顺序固定、字段名一字不差、每字段独占一行、单元格内不换行**（需要多条内容时用 `<br>` 分隔）：

| 序号 | 字段 | 格式 | 正则约束 | 说明 |
|:--:|:---|:---|:---|:---|
| 1 | 用例编号 | 单行 | `^TC-[A-Z0-9]+-\d{3}$` | 模块内唯一且不复用 |
| 2 | 测试目的 | 单行 | 自由文本 | 一句话，指向**单一**验证目标 |
| 3 | 前置条件 | 单行 | 自由文本 | 只描述**环境状态**；「必须先执行某用例」属依赖用例字段，不在此重复 |
| 4 | 依赖用例 | 单行 | `^无$` 或 `^TC-[A-Z0-9]+-\d{3}(, TC-[A-Z0-9]+-\d{3})*$` | 见下方规范 |
| 5 | 操作步骤 | 条目列表 | 每条目以 `^\d+\. ` 开头，条目间以 `<br>` 分隔 | 自 1 起连续编号 |
| 6 | 预期结果 | 条目列表 | 同操作步骤；编号是步骤号的**子集** | 见 §1.6 |

**依赖用例字段规范（四条硬性要求）：**

1. 无依赖时**必须**写 `无`，不得留空、不得写 `-`、`N/A`、`none` 等变体。
2. 有依赖时写**用例编号全称**（含模块段），不写省略形式；跨模块依赖同样写全称。
3. 多项依赖以**半角逗号加一个空格**（`, `）分隔；逗号前后不得出现空格以外的字符。
4. 多项依赖的**排列顺序即依赖先后**——先决者在前。因用例按编号升序声明，等价于按被依赖用例的**编号升序**排列。

**模块级约定**：本模块两个载体的构建步骤统一置于 `TC-EVENT-001` 的步骤 1（该步骤一次构建两个目标）；其余用例不在步骤中重复构建，而是通过依赖用例字段间接依赖其产出的已构建载体。

**为什么必须统一格式**：本文件由 `tools/check/check_manual_test_format.py` 解析守护（CTest 用例 `check_manual_test_format`，校验字段名与顺序、取值域、依赖拓扑、步骤-预期同号映射与执行记录表格式）。上述约定使每个字段可用**单条正则**校验，无需自然语言推断。

### 1.6 操作步骤与预期结果编号规则

**预期结果与操作步骤同号一一对应**：`预期结果 N` 即「步骤 N 的对应结果」。

1. 预期结果的序号**等于**其对应步骤的序号，两者严格相等——不得因省略而重排、压缩或重新连续编号。
2. 纯执行步骤**不产生预期结果**，其序号在预期结果列表中直接跳过。
3. 故预期结果的序号**不要求连续**，条数也无需等于步骤数。
4. 步骤列表中，纯执行步骤在文本末尾标注「（纯执行，无预期结果）」，供与预期结果列表逐号对照。

示例：某用例有步骤 1、2、3、4，其中步骤 3 为纯执行步骤，则该用例的预期结果共 3 条，编号为 1、2、4。

**该规则解决的问题**：执行者按步骤顺序推进时，编号本身即是映射关系，无需自行推断「这条预期结果对应哪一步」；执行记录可借步骤号精确落到失败点。

### 1.7 判定与记录

| 结果 | 含义 |
|:---|:---|
| PASS | 全部非空预期结果均满足 |
| FAIL | 任一非空预期结果不满足；须在记录表登记该预期结果对应的**步骤号**、实际现象与缺陷编号 |
| BLOCKED | 前置条件无法满足（如载体构建失败），未开始执行 |
| SKIP | 主动判定不适用于本环境（如被测平台与用例限定平台不符） |

输出中含**时间戳、线程 id、行号**的字段属可变部分，判定时只核对格式与是否出现，不比对具体值。

**执行记录表格式**（列顺序固定）：

| 列 | 格式 | 允许值 / 正则 |
|:---|:---|:---|
| 用例编号 | 单行 | 与 §2 用例编号一致 |
| 执行日期 | 单行 | `^\d{4}-\d{2}-\d{2}$` |
| 执行人 | 单行 | 自由文本 |
| 结果 | 单行 | `PASS` / `FAIL` / `BLOCKED` / `SKIP` |
| 失败步骤号 | 单行 | 留空（未失败或未执行），或 `^\d+(, \d+)*$`（多个以半角逗号加一个空格分隔） |
| 实际现象 | 单行 | 自由文本 |
| 缺陷编号 | 单行 | 留空或缺陷单号 |
| 备注 | 单行 | 自由文本 |

未执行的取值一律**留空单元格**，不写 `-`、`TBD`、`N/A` 等占位符。

### 1.8 覆盖取舍

本模块按「人工判定是否优于程序判定」取舍。

**纳入人工测试的子域**：

| 子域 | 人工判定优于程序判定的原因 |
|:---|:---|
| 指针捕获 | 「按住后移出控件、松开在原位」这一链路把「事件仍回到原控件」与「事件转投新命中控件」两种实现区分开来，合成事件无法复现 |
| 多指针独立路由 | 多根手指同时作用于不同控件时的隔离性，需真实多触点 |
| 高频派发完整性 | 是否存在丢事件或重复派发，需真实输入速率 |
| 焦点遍历顺序 | 是否与视觉顺序一致属人因判断，程序只能断言遍历集合 |
| 键码表稳定性 | 枚举取值不得在版本间漂移，跨运行一致是必要条件 |

**不纳入人工测试的头及其理由**：

| 头 | 理由 | 已有覆盖 |
|:---|:---|:---|
| `event.h` | 事件对象为纯数据结构，字段与取值可穷举断言 | `utest_event` |
| `dispatcher.h` | 分发顺序、冒泡与拦截为确定性逻辑 | `utest_dispatcher` `itest_keyboard_nav` `itest_context_menu_tooltip` |
| `focus.h` | 焦点状态机与作用域栈为确定性逻辑；其**遍历顺序的合理性**由 TC-EVENT-007 覆盖 | `utest_focus` `itest_text_focus` |
| `gesture.h` | 识别器输出为数值（缩放比例、旋转弧度、位移、主轴锁定），程序判定更精确 | `utest_gesture` |
| `drag_drop.h` | 拖放数据传递为确定性流程 | `utest_drag_drop` |
| `keycode.h` | 键码映射表为静态查表 | `utest_keycode` |

## 2 用例清单

### 2.1 载体基线

#### TC-EVENT-001 载体构建与启动基线

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-EVENT-001 |
| 测试目的 | 确认本模块两个载体均可构建、启动并正常显示窗口，为后续用例提供基线 |
| 前置条件 | 位于仓库根目录；`build/` 已完成 CMake 配置；本机工具链可用 |
| 依赖用例 | 无 |
| 操作步骤 | 1. 构建载体：`cmake --build build --target demo_event demo_multitouch`（纯执行，无预期结果）<br>2. 启动 `./build/demo_event.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>3. 查看窗口标题栏文本<br>4. 查看 `err.txt` 的内容与行数<br>5. 关闭窗口并记录进程退出码<br>6. 以同法启动 `./build/demo_multitouch.exe`，查看其窗口标题与 `err.txt` |
| 预期结果 | 3. 标题为 `Event · Aurora Demo`<br>4. 恰 1 行，级别为 `INF`，消息含 `[run_demo] window shown: Event · Aurora Demo`；此时**尚不含**任何点击或拖拽日志<br>5. 退出码为 0，`err.txt` 中无 `ERR` 或 `FTL` 级日志<br>6. `demo_multitouch` 标题为 `Multi-Touch · Aurora Demo`，`err.txt` 同为恰 1 行 `INF` 级 `[run_demo] window shown: Multi-Touch · Aurora Demo` |

### 2.2 指针事件与命中

#### TC-EVENT-002 点击派发到命中控件，落空不触发

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-EVENT-002 |
| 测试目的 | 验证事件按命中测试派发到目标控件，且点击空白区域不会误触发该控件回调 |
| 前置条件 | 载体 `demo_event` 已构建成功；当前目录可写；鼠标可用 |
| 依赖用例 | TC-EVENT-001 |
| 操作步骤 | 1. 启动 `./build/demo_event.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>2. 在窗口内标注 `clickable area` 的方块中央单击一次（纯执行，无预期结果）<br>3. 查看 `err.txt` 中新增行的分类与消息<br>4. 在方块之外、卡片之内的空白处单击一次（纯执行，无预期结果）<br>5. 再次查看 `err.txt` 的行数变化<br>6. 读取窗口内 `KeyCode::Enter =` 行的内容 |
| 预期结果 | 3. 新增恰 1 行，级别为 `INF`，分类为 `demo`，消息含 `[event] clicked (MouseEvent dispatched to this widget)`<br>5. 行数与步骤 3 之后保持一致，**无新增**（点击落空不触发该控件回调）<br>6. 显示 `KeyCode::Enter = ` 后接一个十进制非负整数；该值与 `include/aurora/event/keycode.h` 中 `KeyCode::Enter` 的枚举声明一致 |

#### TC-EVENT-003 拖拽在按下控件内持续触发，且不溢出到其他控件

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-EVENT-003 |
| 测试目的 | 验证拖拽回调绑定在按下时的命中控件上，拖动过程中不转投相邻控件 |
| 前置条件 | 载体 `demo_multitouch` 已构建成功；鼠标可用；两个拖拽方块均可见 |
| 依赖用例 | TC-EVENT-001 |
| 操作步骤 | 1. 启动 `./build/demo_multitouch.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>2. 在 `Drag me A` 方块中央按下左键，保持按住水平拖动约 100 像素，然后松开（纯执行，无预期结果）<br>3. 查看 `err.txt` 中 `[drag]` 行的内容与所属标识<br>4. 在 `Drag me B` 方块上重复一次同样的拖动（纯执行，无预期结果）<br>5. 查看 `err.txt` 中新增的 `[drag]` 行 |
| 预期结果 | 3. 出现若干条 `[drag] A`（拖动过程中**持续**产出，非仅按下或松开时各一条），且**不含** `[drag] B`<br>5. 新增行全部为 `[drag] B`，不含 `[drag] A`（每次拖拽仅归属按下时命中的控件） |

#### TC-EVENT-004 按住后移出控件，松开前事件仍回到原控件

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-EVENT-004 |
| 测试目的 | 验证拖拽期间指针被捕获：光标离开按下控件后，移动事件仍送回原控件直至松开 |
| 前置条件 | 载体 `demo_multitouch` 已构建成功；鼠标可用；窗口可移出屏幕边缘或以其他窗口覆盖 |
| 依赖用例 | TC-EVENT-001, TC-EVENT-003 |
| 操作步骤 | 1. 启动 `./build/demo_multitouch.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>2. 在 `Drag me A` 上按下左键，保持按住把光标移到 `Drag me B` 上方（纯执行，无预期结果）<br>3. 查看 `err.txt` 中此时的 `[drag]` 行<br>4. 继续按住并把光标移出窗口客户区之外，再移动若干像素（纯执行，无预期结果）<br>5. 查看 `err.txt` 中此时的 `[drag]` 行数量变化<br>6. 松开左键，再移动鼠标若干像素，查看 `err.txt` |
| 预期结果 | 3. 仍为 `[drag] A`，**未切换**为 `[drag] B`（捕获期间事件不按当前位置重新命中）<br>5. 行数继续增长（光标已在控件之外，仍持续收到移动事件），证明捕获生效<br>6. 松手后移动鼠标**不再产生**新的 `[drag]` 行 |

### 2.3 高频派发

#### TC-EVENT-005 高频点击不重复派发，丢失率可接受

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-EVENT-005 |
| 测试目的 | 验证快速连续点击下事件不重复派发，且丢失率在可接受范围内 |
| 前置条件 | 载体 `demo_event` 已构建成功；鼠标可用；可辨认点击次数 |
| 依赖用例 | TC-EVENT-001, TC-EVENT-002 |
| 操作步骤 | 1. 启动 `./build/demo_event.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>2. 在 `clickable area` 方块内以约每秒 5 次的节奏连续点击 20 次（纯执行，无预期结果）<br>3. 统计 `err.txt` 中 `[event] clicked` 的行数<br>4. 计算行数与实际点击次数之差 |
| 预期结果 | 3. 行数**不多于**实际点击次数（无重复派发）<br>4. 丢失次数不超过实际点击次数的 10%；超过则 FAIL（记录实际点击次数与行数） |

### 2.4 多点触控

#### TC-EVENT-006 多指并发路由与原始流触点计数

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-EVENT-006 |
| 测试目的 | 验证触摸原始流交付的触点数量与活跃数与实际相符，且多指作用于不同控件时各自独立 |
| 前置条件 | 载体 `demo_multitouch` 已构建成功；**需多点触控硬件**，无该硬件时记 `SKIP` |
| 依赖用例 | TC-EVENT-001 |
| 操作步骤 | 1. 启动 `./build/demo_multitouch.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>2. 单指触摸 `Raw stream listener` 区域并保持，查看 `err.txt` 中 `[multitouch]` 行<br>3. 在保持第一指的同时，第二指触摸同一区域，再次查看<br>4. 抬起第二指，再次查看<br>5. 全部抬起后再查看 |
| 预期结果 | 2. 出现 `[multitouch] points=` 后接触点数的行，此时 ` active=` 为 1<br>3. 新行的触点数与活跃数均变为 2<br>4. 新行的活跃数回落到 1<br>5. 不再产生新行（或 `active=` 为 0），且期间无 `ERR` / `FTL` 级日志 |

### 2.5 焦点遍历

#### TC-EVENT-007 Tab 焦点遍历顺序与视觉顺序一致

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-EVENT-007 |
| 测试目的 | 验证键盘焦点遍历顺序符合视觉排布，且遍历完整、不重复 |
| 前置条件 | 载体 `demo_multitouch` 已构建成功；键盘可用 |
| 依赖用例 | TC-EVENT-001 |
| 操作步骤 | 1. 启动 `./build/demo_multitouch.exe`（纯执行，无预期结果）<br>2. 反复按 `Tab` 键，逐个记录焦点依次落在哪些控件上（纯执行，无预期结果）<br>3. 对照窗口的视觉排布，核对遍历顺序<br>4. 继续按 `Tab` 直至回到起始控件，统计遍历一轮经过的控件数量<br>5. 按 `Shift+Tab` 若干次，记录反向遍历顺序 |
| 预期结果 | 3. 遍历顺序与视觉顺序一致：先落在 `Drag me A`，再 `Drag me B`，最后 `Raw stream listener`；不出现跳序<br>4. 一轮经过的控件数量等于窗口内可聚焦控件总数（无跳过、无重复进入同一控件）<br>5. 反向遍历顺序与正向相反，且不跳过任何控件 |

## 3 执行记录表

| 用例编号 | 执行日期 | 执行人 | 结果 | 失败步骤号 | 实际现象 | 缺陷编号 | 备注 |
|:---|:---|:---|:---|:---|:---|:---|:---|
| TC-EVENT-001 | | | | | | | |
| TC-EVENT-002 | | | | | | | |
| TC-EVENT-003 | | | | | | | |
| TC-EVENT-004 | | | | | | | |
| TC-EVENT-005 | | | | | | | |
| TC-EVENT-006 | | | | | | | |
| TC-EVENT-007 | | | | | | | |

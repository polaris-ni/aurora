# navigation 模块人工测试用例

> **文档状态：临时**
> 本文件按固定格式编写，供程序解析：字段顺序、字段名、值域与分隔符均为约定的一部分，见 §1.5。
> 格式规范与首份示例见 `codespec/manual-test/01-core.md`。

## 1 适用范围与约定

### 1.1 被测模块

`navigation/` —— 导航层（6 个公共头），在实测依赖分层中位于 L6。

| 头 | 职责 |
|:---|:---|
| `route.h` | 路由（页面）值对象与路由注册表 |
| `navigator.h` | 导航栈与压栈/弹栈 |
| `navigator_host.h` | 宿主：监听导航栈并把页面装配到视图，含深层链接 `open_uri` |
| `router.h` | 按名称解析路由构造器 |
| `transition_layer.h` | 转场覆盖层 |
| `hero.h` | 共享元素转场 |

本模块的人工增益集中在**页面切换的观感**：转场动画是否连贯、共享元素是否「飞」到正确位置、反向转场是否对称。这些都是连续时间轴上的视觉性质，程序能断言导航栈的深度与当前路由名，但断言不了「共享元素飞入过程是否出现闪现或跳位」。

### 1.2 执行载体

复用既有示例程序，不新增载体：

| 载体 | 构建目标 | 用途 |
|:---|:---|:---|
| `examples/demos/demo_navigation.cpp` | `demo_navigation` | 导航栈深度、当前路由、路由注册表查询（静态展示） |
| `examples/demos/demo_navigator.cpp` | `demo_navigator` | 深层链接：一处 URI 重建整栈 |
| `examples/demos/demo_hero.cpp` | `demo_hero` | 共享元素正向与反向转场 |

`demo_navigator` 与 `demo_hero` 经 `au::Application` 驱动 `au::Animator`（而非 `run_demo`），以便转场动画随帧演进；因此它们**不产生** `[run_demo] window shown` 提示行，判定启动时不要套用该特征。

### 1.3 执行环境

| 项目 | 要求 |
|:---|:---|
| 平台 | Windows 本机（实测环境为 MinGW/GCC + Ninja） |
| 构建 | 库已构建；三个载体为按需目标，不在默认构建内 |
| 终端 | 支持 UTF-8 输出 |
| 分离输出 | 诊断日志写 stderr |
| 显示 | 转场类用例需可清晰观察动画过程 |
| 指针 | 真实鼠标 |

### 1.4 用例编号规则

格式 `TC-<模块段>-<三位序号>`，自 001 起连续编号。
模块段为**模块目录名的大写形式**（`core` → `CORE`、`i18n` → `I18N`；取 `include/aurora/` 下的模块目录名，本目录文件名前缀 `NN-` 是文档序号、不计入模块段，如 `01-core.md` → `CORE`），**不得自创缩写**——目录名含数字时数字原样保留，故正则为 `^TC-[A-Z0-9]+-\d{3}$`。
本模块目录名 `navigation`，即 `TC-NAVIGATION-001` 起。序号在本模块内唯一且**不复用**；用例被删除后其编号作废，新用例取下一个可用序号。
用例在文档中**按编号升序声明**——该顺序即默认执行顺序，也是依赖列表排序的依据（见 §1.5）。

### 1.5 用例字段清单与格式规范

每条用例由**六个固定字段**组成，排布于同一张 Markdown 表格内。**字段顺序固定、字段名一字不差、每字段独占一行、单元格内不换行**（需要多条内容时用 `<br>` 分隔）：

| 序号 | 字段 | 格式 | 正则约束 | 说明 |
|:--:|:---|:---|:---|:---|
| 1 | 用例编号 | 单行 | `^TC-[A-Z0-9]+-\d{3}$` | 模块内唯一且不复用 |
| 2 | 测试目的 | 单行 | 自由文本 | 一句话，指向**单一**验证目标 |
| 3 | 前置条件 | 单行 | 自由文本 | 只描述**环境状态**；「必须先执行某用例」属依赖用例字段，不在此重复 |
| 4 | 依赖用例 | 单行 | `^无$` 或 `^TC-[A-Z0-9]+-\d{3}(, TC-[A-Z0-9]+-\d{3})*$` | 见下方规范 |
| 5 | 操作步骤 | 条目列表 | 每条目以 `^\d+\. ` 开头，条目间以 `<br>` 分隔 | 自 1 起连续编号 |
| 6 | 预期结果 | 条目列表 | 同操作步骤；编号是步骤号的**子集** | 见 §1.6 |

**依赖用例字段规范（四条硬性要求）：**

1. 无依赖时**必须**写 `无`，不得留空、不得写 `-`、`N/A`、`none` 等变体。
2. 有依赖时写**用例编号全称**（含模块段），不写省略形式；跨模块依赖同样写全称。
3. 多项依赖以**半角逗号加一个空格**（`, `）分隔；逗号前后不得出现空格以外的字符。
4. 多项依赖的**排列顺序即依赖先后**——先决者在前。因用例按编号升序声明，等价于按被依赖用例的**编号升序**排列。

**模块级约定**：本模块三个载体的构建步骤统一置于 `TC-NAVIGATION-001` 的步骤 1（该步骤一次构建三个目标）；其余用例不在步骤中重复构建，而是通过依赖用例字段间接依赖其产出的已构建载体。

**为什么必须统一格式**：本文件由 `tools/check/check_manual_test_format.py` 解析守护（CTest 用例 `check_manual_test_format`，校验字段名与顺序、取值域、依赖拓扑、步骤-预期同号映射与执行记录表格式）。上述约定使每个字段可用**单条正则**校验，无需自然语言推断。

### 1.6 操作步骤与预期结果编号规则

**预期结果与操作步骤同号一一对应**：`预期结果 N` 即「步骤 N 的对应结果」。

1. 预期结果的序号**等于**其对应步骤的序号，两者严格相等——不得因省略而重排、压缩或重新连续编号。
2. 纯执行步骤**不产生预期结果**，其序号在预期结果列表中直接跳过。
3. 故预期结果的序号**不要求连续**，条数也无需等于步骤数。
4. 步骤列表中，纯执行步骤在文本末尾标注「（纯执行，无预期结果）」，供与预期结果列表逐号对照。

示例：某用例有步骤 1、2、3、4，其中步骤 3 为纯执行步骤，则该用例的预期结果共 3 条，编号为 1、2、4。

**该规则解决的问题**：执行者按步骤顺序推进时，编号本身即是映射关系，无需自行推断「这条预期结果对应哪一步」；执行记录可借步骤号精确落到失败点。

### 1.7 判定与记录

| 结果 | 含义 |
|:---|:---|
| PASS | 全部非空预期结果均满足 |
| FAIL | 任一非空预期结果不满足；须在记录表登记该预期结果对应的**步骤号**、实际现象与缺陷编号 |
| BLOCKED | 前置条件无法满足（如载体构建失败），未开始执行 |
| SKIP | 主动判定不适用于本环境（如被测平台与用例限定平台不符） |

输出中含**时间戳、线程 id、行号**的字段属可变部分，判定时只核对格式与是否出现，不比对具体值。

**执行记录表格式**（列顺序固定）：

| 列 | 格式 | 允许值 / 正则 |
|:---|:---|:---|
| 用例编号 | 单行 | 与 §2 用例编号一致 |
| 执行日期 | 单行 | `^\d{4}-\d{2}-\d{2}$` |
| 执行人 | 单行 | 自由文本 |
| 结果 | 单行 | `PASS` / `FAIL` / `BLOCKED` / `SKIP` |
| 失败步骤号 | 单行 | 留空（未失败或未执行），或 `^\d+(, \d+)*$`（多个以半角逗号加一个空格分隔） |
| 实际现象 | 单行 | 自由文本 |
| 缺陷编号 | 单行 | 留空或缺陷单号 |
| 备注 | 单行 | 自由文本 |

未执行的取值一律**留空单元格**，不写 `-`、`TBD`、`N/A` 等占位符。

### 1.8 覆盖取舍

本模块按「人工判定是否优于程序判定」取舍，不追求对 6 个头的全覆盖。

**纳入人工测试的子域**：

| 子域 | 人工判定优于程序判定的原因 |
|:---|:---|
| 深层链接重建整栈 | 「一处 URI 跳多级后栈内容是否正确」需对照界面逐级核对 |
| 共享元素转场 | 元素飞行的轨迹、尺寸插值与淡变是否连贯，属连续观感 |
| 反向转场对称性 | 「返回时动画是否与进入时互为逆过程」属对称性判断 |
| 栈深度与当前路由 | 静态展示即可核对，且是上述交互用例的前置事实 |

**不纳入人工测试的头及其理由**：

| 头 | 理由 | 已有覆盖 |
|:---|:---|:---|
| `route.h` | 路由值对象与注册表为确定性容器操作 | `utest_route` |
| `navigator.h` | 压栈/弹栈与深度维护为确定性栈逻辑；其**深层链接后的栈内容**由 TC-NAVIGATION-004 覆盖 | `utest_navigator` |
| `navigator_host.h` | 宿主装配逻辑为确定性；其**转场演进**由 TC-NAVIGATION-005 与 TC-NAVIGATION-006 覆盖 | `utest_navigator_host` `itest_nav_win` |
| `router.h` | 按名称解析构造器为确定性查表 | `utest_router` |
| `transition_layer.h` | 覆盖层的绘制与裁剪为确定性几何 | `utest_transition_layer` |
| `hero.h` | 共享元素的标签匹配与矩形插值计算为确定性；其**飞行观感**由 TC-NAVIGATION-005 与 TC-NAVIGATION-006 覆盖 | `utest_hero` |

## 2 用例清单

### 2.1 载体基线

#### TC-NAVIGATION-001 载体构建与启动基线

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-NAVIGATION-001 |
| 测试目的 | 确认本模块三个载体均可构建启动，并记录两类启动特征（经 `run_demo` 与经 `Application`）的差异 |
| 前置条件 | 位于仓库根目录；`build/` 已完成 CMake 配置；本机工具链可用 |
| 依赖用例 | 无 |
| 操作步骤 | 1. 构建载体：`cmake --build build --target demo_navigation demo_navigator demo_hero`（纯执行，无预期结果）<br>2. 启动 `./build/demo_navigation.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>3. 查看窗口标题与 `err.txt` 行数<br>4. 关闭该窗口后启动 `./build/demo_navigator.exe 1> out2.txt 2> err2.txt`（纯执行，无预期结果）<br>5. 查看 `err2.txt` 的行数 |
| 预期结果 | 3. 标题为 `Navigation · Aurora Demo`；`err.txt` 恰 1 行 `INF` 级 `[run_demo] window shown: Navigation · Aurora Demo`<br>5. `err2.txt` 为 **0 行**——`demo_navigator` 经 `au::Application` 自行驱动帧循环，不产生 `run_demo` 提示（**属预期差异，不得判为缺陷**） |

### 2.2 导航栈与路由表

#### TC-NAVIGATION-002 导航栈深度与当前路由一致

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-NAVIGATION-002 |
| 测试目的 | 验证压栈后栈深度与当前路由的取值相互自洽，且与界面上呈现的页面一致 |
| 前置条件 | 载体 `demo_navigation` 已构建成功 |
| 依赖用例 | TC-NAVIGATION-001 |
| 操作步骤 | 1. 启动 `./build/demo_navigation.exe`（纯执行，无预期结果）<br>2. 读取窗口内 `Navigator depth =` 行的数值与 `current route =` 行的名称<br>3. 核对两值是否自洽（当前路由应是栈顶）<br>4. 对照窗口标题区显示的页面标题与步骤 2 的当前路由名 |
| 预期结果 | 2. `Navigator depth = 2`、`current route = about`<br>3. 两者自洽：深度为 2 表示栈内有 `home` 与 `about` 两页，栈顶即 `about`（源码中先以 `home` 构造导航器、随后 `push(about)`）<br>4. 标题区显示 `About`，与当前路由名 `about` 对应，不存在「标题仍是 Home 而路由已是 about」的不一致 |

#### TC-NAVIGATION-003 路由注册表查询返回登记结果

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-NAVIGATION-003 |
| 测试目的 | 验证路由注册表的查询结果反映实际登记状态，未登记的名称不会被误判为已存在 |
| 前置条件 | 载体 `demo_navigation` 已构建成功 |
| 依赖用例 | TC-NAVIGATION-001 |
| 操作步骤 | 1. 启动 `./build/demo_navigation.exe`（纯执行，无预期结果）<br>2. 读取窗口内 `Router has 'home' =` 行的取值<br>3. 对照源码中 `register_route` 的调用（纯执行，无预期结果）<br>4. 核对查询结果与登记状态是否一致 |
| 预期结果 | 2. 显示 `Router has 'home' = true`<br>4. 与源码一致：源码中经 `register_route("home", ...)` 登记过 `home`，故查询为 `true`；若显示 `false` 说明登记未生效，即 FAIL |

### 2.3 深层链接

#### TC-NAVIGATION-004 一处 URI 重建整条导航栈

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-NAVIGATION-004 |
| 测试目的 | 验证深层链接能按 URI 的路径段一次性重建整栈，而非仅追加一层 |
| 前置条件 | 载体 `demo_navigator` 已构建成功；鼠标可用 |
| 依赖用例 | TC-NAVIGATION-001 |
| 操作步骤 | 1. 启动 `./build/demo_navigator.exe`（纯执行，无预期结果）<br>2. 记录初始页面显示的内容（应为 `Home`）（纯执行，无预期结果）<br>3. 点击标注 `Open home/detail/settings (deep link)` 的按钮（纯执行，无预期结果）<br>4. 观察跳转过程中是否出现多级页面连续切换的观感，并记录最终显示的页面<br>5. 检查最终页面的内容与 URI 末段的关系 |
| 预期结果 | 4. 跳转后最终显示 `Settings` 页面；过程中**不应**出现先闪出 `Detail` 再闪出 `Settings` 的逐级跳转观感（该操作是重建整栈，一次到位）<br>5. 最终页面内容为 `Settings`，与 URI 末段 `settings` 对应（而非停留在中间段 `detail`） |

### 2.4 共享元素转场

#### TC-NAVIGATION-005 共享元素正向转场形变飞入

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-NAVIGATION-005 |
| 测试目的 | 验证同标签的共享元素在两页之间以形变飞行的方式过渡，而非淡出淡入式的硬切换 |
| 前置条件 | 载体 `demo_hero` 已构建成功；鼠标可用；可清晰观察动画 |
| 依赖用例 | TC-NAVIGATION-001 |
| 操作步骤 | 1. 启动 `./build/demo_hero.exe`（纯执行，无预期结果）<br>2. 记录首页中标注 `Aurora` 的色块的位置与尺寸（纯执行，无预期结果）<br>3. 点击首页的跳转按钮（纯执行，无预期结果）<br>4. 注视 `Aurora` 色块在转场过程中的位置与尺寸变化<br>5. 记录转场结束后该色块的位置、尺寸与颜色 |
| 预期结果 | 4. 色块**连续地**从首页位置移动到详情页位置，同时尺寸逐步变大——移动过程可见中间位置与中间尺寸，无瞬间闪现、无跳位<br>5. 转场结束后色块位于详情页的对应位置，尺寸明显大于首页时（源码中首页色块较小、详情页为 160×120），颜色为详情页的绿色；全程无重影残留 |

#### TC-NAVIGATION-006 反向转场与导航栈回退同步

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-NAVIGATION-006 |
| 测试目的 | 验证返回操作同时完成栈回退与反向转场，两者时序一致（不出现「页面已换但动画还在演」或反之） |
| 前置条件 | 载体 `demo_hero` 已构建成功；鼠标可用；已按 TC-NAVIGATION-005 完成一次正向转场 |
| 依赖用例 | TC-NAVIGATION-001, TC-NAVIGATION-005 |
| 操作步骤 | 1. 在详情页点击标注 `Back` 的按钮（纯执行，无预期结果）<br>2. 注视色块的移动方向与尺寸变化<br>3. 记录转场结束后显示的页面内容<br>4. 记录色块最终的位置、尺寸与颜色 |
| 预期结果 | 2. 色块沿与正向转场**相反**的方向移动并逐步缩小，路径与正向大致重合（互为逆过程）<br>3. 转场结束后回到首页，显示 `Aurora` 色块与跳转按钮<br>4. 色块回到首页的位置与尺寸，颜色为首页时的颜色；返回后不残留详情页的任何元素 |

## 3 执行记录表

| 用例编号 | 执行日期 | 执行人 | 结果 | 失败步骤号 | 实际现象 | 缺陷编号 | 备注 |
|:---|:---|:---|:---|:---|:---|:---|:---|
| TC-NAVIGATION-001 | | | | | | | |
| TC-NAVIGATION-002 | | | | | | | |
| TC-NAVIGATION-003 | | | | | | | |
| TC-NAVIGATION-004 | | | | | | | |
| TC-NAVIGATION-005 | | | | | | | |
| TC-NAVIGATION-006 | | | | | | | |

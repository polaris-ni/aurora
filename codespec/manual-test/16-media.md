# media 模块人工测试用例

> **文档状态：临时**
> 本文件按固定格式编写，供程序解析：字段顺序、字段名、值域与分隔符均为约定的一部分，见 §1.5。
> 格式规范与首份示例见 `codespec/manual-test/01-core.md`。

## 1 适用范围与约定

### 1.1 被测模块

`media/` —— 媒体层（7 个公共头），在实测依赖分层中位于 L6。

| 头 | 职责 |
|:---|:---|
| `video_player.h` | 视频播放器控件（继承 `Container`，可叠子节点），含播放控制与适配模式 |
| `video_source.h` | 解码源抽象 |
| `video_controls.h` | 控件叠层（播放/暂停、进度条、时间、音量） |
| `image_sequence_source.h` | 内置零依赖源：由一组图像按时长顺序播放 |
| `audio.h` `audio_sink_bridge.h` | 音频上下文与输出桥 |

本模块的人工增益集中在**播放的连续观感**：画面是否随时间平滑推进、滤镜是否真实作用到每一帧、控件叠层的交互是否顺手。程序能断言「源返回了第 N 帧」，但断言不了「这 48 帧在屏幕上看起来是不是一段连贯的动画」。

### 1.2 执行载体

复用既有示例程序，不新增载体：

| 载体 | 构建目标 | 用途 |
|:---|:---|:---|
| `examples/demos/demo_video_player.cpp` | `demo_video_player` | 逐帧播放、子类化滤镜、控件叠层 |

该载体使用 `ImageSequenceSource` 生成 48 帧变色画面（24 fps，故完整周期 2 秒），并以 `TintedVideoPlayer` 覆写 `on_frame` 对每帧叠加青色调后交给基类缓存与重绘。因此同一次观测可同时验证「帧推进」与「子类化钩子生效」。

### 1.3 执行环境

| 项目 | 要求 |
|:---|:---|
| 平台 | Windows 本机（实测环境为 MinGW/GCC + Ninja） |
| 构建 | 库已构建；载体为按需目标，不在默认构建内 |
| 终端 | 支持 UTF-8 输出 |
| 分离输出 | 诊断日志写 stderr |
| 显示 | 需可清晰观察连续色彩变化的显示器 |
| 指针与键盘 | 真实鼠标；控件叠层的键盘快捷键（若有）需键盘 |

本模块**不含**音频通路的人工验证：本机未接入音频输出设备，且本模块的音频头（`audio.h`、`audio_sink_bridge.h`）对应单测已覆盖其数据通路，人工判定「声音是否正确」缺少可复现的判定基准，故不立用例（见 §1.8）。

### 1.4 用例编号规则

格式 `TC-<模块段>-<三位序号>`，自 001 起连续编号。
模块段为**模块目录名的大写形式**（`core` → `CORE`、`i18n` → `I18N`；取 `include/aurora/` 下的模块目录名，本目录文件名前缀 `NN-` 是文档序号、不计入模块段，如 `01-core.md` → `CORE`），**不得自创缩写**——目录名含数字时数字原样保留，故正则为 `^TC-[A-Z0-9]+-\d{3}$`。
本模块目录名 `media`，即 `TC-MEDIA-001` 起。序号在本模块内唯一且**不复用**；用例被删除后其编号作废，新用例取下一个可用序号。
用例在文档中**按编号升序声明**——该顺序即默认执行顺序，也是依赖列表排序的依据（见 §1.5）。

### 1.5 用例字段清单与格式规范

每条用例由**六个固定字段**组成，排布于同一张 Markdown 表格内。**字段顺序固定、字段名一字不差、每字段独占一行、单元格内不换行**（需要多条内容时用 `<br>` 分隔）：

| 序号 | 字段 | 格式 | 正则约束 | 说明 |
|:--:|:---|:---|:---|:---|
| 1 | 用例编号 | 单行 | `^TC-[A-Z0-9]+-\d{3}$` | 模块内唯一且不复用 |
| 2 | 测试目的 | 单行 | 自由文本 | 一句话，指向**单一**验证目标 |
| 3 | 前置条件 | 单行 | 自由文本 | 只描述**环境状态**；「必须先执行某用例」属依赖用例字段，不在此重复 |
| 4 | 依赖用例 | 单行 | `^无$` 或 `^TC-[A-Z0-9]+-\d{3}(, TC-[A-Z0-9]+-\d{3})*$` | 见下方规范 |
| 5 | 操作步骤 | 条目列表 | 每条目以 `^\d+\. ` 开头，条目间以 `<br>` 分隔 | 自 1 起连续编号 |
| 6 | 预期结果 | 条目列表 | 同操作步骤；编号是步骤号的**子集** | 见 §1.6 |

**依赖用例字段规范（四条硬性要求）：**

1. 无依赖时**必须**写 `无`，不得留空、不得写 `-`、`N/A`、`none` 等变体。
2. 有依赖时写**用例编号全称**（含模块段），不写省略形式；跨模块依赖同样写全称。
3. 多项依赖以**半角逗号加一个空格**（`, `）分隔；逗号前后不得出现空格以外的字符。
4. 多项依赖的**排列顺序即依赖先后**——先决者在前。因用例按编号升序声明，等价于按被依赖用例的**编号升序**排列。

**模块级约定**：本模块载体的构建步骤统一置于 `TC-MEDIA-001` 的步骤 1；其余用例不在步骤中重复构建，而是通过依赖用例字段间接依赖其产出的已构建载体。

**为什么必须统一格式**：本文件由 `tools/check/check_manual_test_format.py` 解析守护（CTest 用例 `check_manual_test_format`，校验字段名与顺序、取值域、依赖拓扑、步骤-预期同号映射与执行记录表格式）。上述约定使每个字段可用**单条正则**校验，无需自然语言推断。

### 1.6 操作步骤与预期结果编号规则

**预期结果与操作步骤同号一一对应**：`预期结果 N` 即「步骤 N 的对应结果」。

1. 预期结果的序号**等于**其对应步骤的序号，两者严格相等——不得因省略而重排、压缩或重新连续编号。
2. 纯执行步骤**不产生预期结果**，其序号在预期结果列表中直接跳过。
3. 故预期结果的序号**不要求连续**，条数也无需等于步骤数。
4. 步骤列表中，纯执行步骤在文本末尾标注「（纯执行，无预期结果）」，供与预期结果列表逐号对照。

示例：某用例有步骤 1、2、3、4，其中步骤 3 为纯执行步骤，则该用例的预期结果共 3 条，编号为 1、2、4。

**该规则解决的问题**：执行者按步骤顺序推进时，编号本身即是映射关系，无需自行推断「这条预期结果对应哪一步」；执行记录可借步骤号精确落到失败点。

### 1.7 判定与记录

| 结果 | 含义 |
|:---|:---|
| PASS | 全部非空预期结果均满足 |
| FAIL | 任一非空预期结果不满足；须在记录表登记该预期结果对应的**步骤号**、实际现象与缺陷编号 |
| BLOCKED | 前置条件无法满足（如载体构建失败），未开始执行 |
| SKIP | 主动判定不适用于本环境（如被测平台与用例限定平台不符） |

输出中含**时间戳、线程 id、行号**的字段属可变部分，判定时只核对格式与是否出现，不比对具体值。

**执行记录表格式**（列顺序固定）：

| 列 | 格式 | 允许值 / 正则 |
|:---|:---|:---|
| 用例编号 | 单行 | 与 §2 用例编号一致 |
| 执行日期 | 单行 | `^\d{4}-\d{2}-\d{2}$` |
| 执行人 | 单行 | 自由文本 |
| 结果 | 单行 | `PASS` / `FAIL` / `BLOCKED` / `SKIP` |
| 失败步骤号 | 单行 | 留空（未失败或未执行），或 `^\d+(, \d+)*$`（多个以半角逗号加一个空格分隔） |
| 实际现象 | 单行 | 自由文本 |
| 缺陷编号 | 单行 | 留空或缺陷单号 |
| 备注 | 单行 | 自由文本 |

未执行的取值一律**留空单元格**，不写 `-`、`TBD`、`N/A` 等占位符。

### 1.8 覆盖取舍

本模块按「人工判定是否优于程序判定」取舍，不追求对 7 个头的全覆盖。

**纳入人工测试的子域**：

| 子域 | 人工判定优于程序判定的原因 |
|:---|:---|
| 帧推进的连续性 | 「48 帧看起来是一段动画而非逐张跳变」属连续时间观感 |
| 子类化钩子的实际效果 | 覆写 `on_frame` 后滤镜是否真的作用于**每一帧**（而非仅首帧），需连续观察 |
| 控件叠层交互 | 播放/暂停按钮的命中、进度条的拖拽手感，需真实指针 |
| 播放期间的稳定性 | 卡顿、撕裂、画面冻结等只在真实运行中出现 |

**不纳入人工测试的头及其理由**：

| 头 | 理由 | 已有覆盖 |
|:---|:---|:---|
| `video_source.h` | 解码源抽象为接口定义，无运行时行为 | `utest_video_source` |
| `image_sequence_source.h` | 按索引与时序取帧为确定性逻辑 | `utest_image_sequence_source` |
| `video_controls.h` | 叠层的布局与绘制为确定性；其**交互手感**由 TC-MEDIA-004 覆盖 | `utest_video_controls` |
| `video_player.h` 的属性与协议面 | 播放状态机、适配模式、属性序列化为确定性逻辑；其**观感**由 TC-MEDIA-002 至 TC-MEDIA-005 覆盖 | `utest_video_player` |
| `audio.h` `audio_sink_bridge.h` | 音频数据通路的正确性由单测覆盖；人工判定「声音是否正确」缺少可复现基准（受设备与主观听感影响） | `utest_audio` `utest_audio_sink_bridge` |

## 2 用例清单

### 2.1 载体基线

#### TC-MEDIA-001 载体构建与启动基线

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-MEDIA-001 |
| 测试目的 | 确认载体可构建、启动并显示视频画面区域，为后续用例提供基线 |
| 前置条件 | 位于仓库根目录；`build/` 已完成 CMake 配置；本机工具链可用 |
| 依赖用例 | 无 |
| 操作步骤 | 1. 构建载体：`cmake --build build --target demo_video_player`（纯执行，无预期结果）<br>2. 启动 `./build/demo_video_player.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>3. 查看窗口标题与客户区尺寸<br>4. 查看窗口内是否呈现彩色画面区域及其位置<br>5. 查看 `err.txt` 的内容与行数 |
| 预期结果 | 3. 标题为 `Video Player`，客户区约为 640×360<br>4. 客户区内呈现一块完整的彩色画面，画面区域与客户区比例一致（源码中播放器尺寸为 640×360，与窗口同为 16:9），四周无异常黑边或留白<br>5. 恰 1 行，级别为 `INF`，消息含 `[run_demo] window shown: Video Player` |

### 2.2 播放的连续性

#### TC-MEDIA-002 画面逐帧推进且随时间连续变化

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-MEDIA-002 |
| 测试目的 | 验证帧按顺序推进并驱动重绘，画面呈现连续变化而非静止或跳变 |
| 前置条件 | 载体 `demo_video_player` 已构建成功 |
| 依赖用例 | TC-MEDIA-001 |
| 操作步骤 | 1. 启动 `./build/demo_video_player.exe`（纯执行，无预期结果）<br>2. 注视画面约 5 秒，观察其颜色变化方式（纯执行，无预期结果）<br>3. 判断颜色变化是否连续<br>4. 记录画面完成一次明显色彩循环所需的时间<br>5. 观察画面中是否出现静止不动的时刻 |
| 预期结果 | 3. 画面颜色**连续变化**（相邻时刻的颜色接近，不是逐张跳变），无长时间停留于同一颜色<br>4. 一次完整色彩循环约为 2 秒（源共 48 帧、按 24 fps 推进）<br>5. 5 秒内画面始终在变化，未出现静止、冻结或黑屏 |

#### TC-MEDIA-003 子类化 on_frame 的滤镜作用于每一帧

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-MEDIA-003 |
| 测试目的 | 验证覆写 `on_frame` 后滤镜对全部帧生效，而非仅首帧（这是子类化播放器的典型陷阱） |
| 前置条件 | 载体 `demo_video_player` 已构建成功；可准确辨认色调 |
| 依赖用例 | TC-MEDIA-001, TC-MEDIA-002 |
| 操作步骤 | 1. 启动 `./build/demo_video_player.exe`（纯执行，无预期结果）<br>2. 注视画面整体色调（纯执行，无预期结果）<br>3. 判断其色偏方向（偏红 / 偏绿 / 偏蓝 / 偏青等）<br>4. 持续观察约 5 秒，逐段核对色调是否始终保持同一偏色方向<br>5. 核对画面颜色是否达到满饱和度的纯色 |
| 预期结果 | 3. 画面整体呈**青色调**——红色分量明显被压低，绿色与蓝色分量相对保留（源码中该滤镜把 R 通道乘 0.6、G 与 B 乘 0.9）<br>4. 5 秒内色调偏色方向**始终一致**，不出现某一帧突然恢复为未染色的原始色（若出现，说明滤镜仅作用于部分帧，即 FAIL）<br>5. 画面颜色未达到满饱和度的纯红/纯绿/纯蓝，仍保留渐变层次（叠加后仍能分辨源帧的色彩变化） |

### 2.3 控件与交互

#### TC-MEDIA-004 控件叠层的播放暂停可用

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-MEDIA-004 |
| 测试目的 | 验证默认控件叠层可见且其播放/暂停操作真实作用于播放状态 |
| 前置条件 | 载体 `demo_video_player` 已构建成功；鼠标可用 |
| 依赖用例 | TC-MEDIA-001, TC-MEDIA-002 |
| 操作步骤 | 1. 启动 `./build/demo_video_player.exe`，确认画面正在变化（纯执行，无预期结果）<br>2. 找到控件叠层中的播放/暂停按钮并点击一次（纯执行，无预期结果）<br>3. 观察画面是否停止变化<br>4. 再次点击同一按钮（纯执行，无预期结果）<br>5. 观察画面是否恢复变化 |
| 预期结果 | 3. 画面**立刻停止**变化，停留于当前帧（非黑屏、非跳回起始帧），此时控件上呈现的状态指示为「已暂停」<br>5. 画面从暂停处**继续**变化，不从头播放（除源的循环语义允许外），控件状态指示恢复为「播放中」 |

#### TC-MEDIA-005 持续播放期间无卡顿与撕裂

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-MEDIA-005 |
| 测试目的 | 验证长时间播放下的稳定性，且画面不出现撕裂与残影 |
| 前置条件 | 载体 `demo_video_player` 已构建成功；可连续观察约 1 分钟 |
| 依赖用例 | TC-MEDIA-001, TC-MEDIA-002 |
| 操作步骤 | 1. 启动 `./build/demo_video_player.exe`（纯执行，无预期结果）<br>2. 让画面持续播放约 1 分钟（纯执行，无预期结果）<br>3. 观察播放是否出现卡顿、停滞或节奏突变<br>4. 观察画面是否存在水平错位的撕裂线或上一帧的残影<br>5. 观察画面区域边缘是否出现溢出的色块 |
| 预期结果 | 3. 一分钟内播放节奏稳定，无卡顿、无冻结、速率无逐渐变慢<br>4. 画面无水平撕裂线，无上一帧残影<br>5. 画面区域边缘干净（无溢出到控件之外的色块） |

## 3 执行记录表

| 用例编号 | 执行日期 | 执行人 | 结果 | 失败步骤号 | 实际现象 | 缺陷编号 | 备注 |
|:---|:---|:---|:---|:---|:---|:---|:---|
| TC-MEDIA-001 | | | | | | | |
| TC-MEDIA-002 | | | | | | | |
| TC-MEDIA-003 | | | | | | | |
| TC-MEDIA-004 | | | | | | | |
| TC-MEDIA-005 | | | | | | | |

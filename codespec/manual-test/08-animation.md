# animation 模块人工测试用例

> **文档状态：临时**
> 本文件按固定格式编写，供程序解析：字段顺序、字段名、值域与分隔符均为约定的一部分，见 §1.5。
> 格式规范与首份示例见 `codespec/manual-test/01-core.md`。

## 1 适用范围与约定

### 1.1 被测模块

`animation/` —— 动画层（4 个公共头），在实测依赖分层中位于 L3。

本模块是**人工判定价值最高的模块之一**：其核心产物是「运动」，而运动是否自然、是否卡顿、是否跳变，程序难以判定——数值断言只能校验某一时刻的计算结果，无法覆盖连续时间轴上的观感。

| 头 | 职责 |
|:---|:---|
| `animator.h` | 动画控制器与补间绑定，按帧推进 |
| `easing.h` | 命名缓动曲线（15 种取值 + 自定义） |
| `spring.h` | 弹簧模拟（刚度 / 阻尼） |
| `timeline.h` | 补间、关键帧、时间轴组合（顺序 / 并行 / 交错） |

### 1.2 执行载体

复用既有示例程序，不新增载体：

| 载体 | 构建目标 | 用途 |
|:---|:---|:---|
| `examples/demos/demo_animation.cpp` | `demo_animation` | 循环动画（背景色往返呼吸、来回缩放）与静态展示值 |

该载体与 `core` 模块的 `demo_core` 有一个值得注意的差异：它经 `au::App()` 启动并自行实现帧循环（`on_frame`），**不走 `run_demo` 提示路径**，因此其 stderr 为空——判定启动基线时不能套用其他 demo 的 `[run_demo] window shown` 日志特征（见 TC-ANIMATION-001）。

### 1.3 执行环境

| 项目 | 要求 |
|:---|:---|
| 平台 | Windows 本机（实测环境为 MinGW/GCC + Ninja） |
| 构建 | 库已构建；载体为按需目标，不在默认构建内 |
| 显示 | 需真实窗口与可清晰观察运动过程的显示器；远程桌面或低刷新率环境会影响流畅度判定 |
| 终端 | 支持 UTF-8 输出 |
| 计时参考 | 本模块多处需目测时长，允许 ±0.3 秒误差；如需更严格计时，可用手机秒表辅助 |

本模块**不含**跨平台后端的动画表现差异（那是 `window` 模块的用例范围）。

### 1.4 用例编号规则

格式 `TC-<模块段>-<三位序号>`，自 001 起连续编号。
模块段为**模块目录名的大写形式**（`core` → `CORE`、`i18n` → `I18N`；取 `include/aurora/` 下的模块目录名，本目录文件名前缀 `NN-` 是文档序号、不计入模块段，如 `01-core.md` → `CORE`），**不得自创缩写**——目录名含数字时数字原样保留，故正则为 `^TC-[A-Z0-9]+-\d{3}$`。
本模块目录名 `animation`，即 `TC-ANIMATION-001` 起。序号在本模块内唯一且**不复用**；用例被删除后其编号作废，新用例取下一个可用序号。
用例在文档中**按编号升序声明**——该顺序即默认执行顺序，也是依赖列表排序的依据（见 §1.5）。

### 1.5 用例字段清单与格式规范

每条用例由**六个固定字段**组成，排布于同一张 Markdown 表格内。**字段顺序固定、字段名一字不差、每字段独占一行、单元格内不换行**（需要多条内容时用 `<br>` 分隔）：

| 序号 | 字段 | 格式 | 正则约束 | 说明 |
|:--:|:---|:---|:---|:---|
| 1 | 用例编号 | 单行 | `^TC-[A-Z0-9]+-\d{3}$` | 模块内唯一且不复用 |
| 2 | 测试目的 | 单行 | 自由文本 | 一句话，指向**单一**验证目标 |
| 3 | 前置条件 | 单行 | 自由文本 | 只描述**环境状态**；「必须先执行某用例」属依赖用例字段，不在此重复 |
| 4 | 依赖用例 | 单行 | `^无$` 或 `^TC-[A-Z0-9]+-\d{3}(, TC-[A-Z0-9]+-\d{3})*$` | 见下方规范 |
| 5 | 操作步骤 | 条目列表 | 每条目以 `^\d+\. ` 开头，条目间以 `<br>` 分隔 | 自 1 起连续编号 |
| 6 | 预期结果 | 条目列表 | 同操作步骤；编号是步骤号的**子集** | 见 §1.6 |

**依赖用例字段规范（四条硬性要求）：**

1. 无依赖时**必须**写 `无`，不得留空、不得写 `-`、`N/A`、`none` 等变体。
2. 有依赖时写**用例编号全称**（含模块段），不写省略形式；跨模块依赖同样写全称。
3. 多项依赖以**半角逗号加一个空格**（`, `）分隔；逗号前后不得出现空格以外的字符。
4. 多项依赖的**排列顺序即依赖先后**——先决者在前。因用例按编号升序声明，等价于按被依赖用例的**编号升序**排列。

**模块级约定**：本模块载体的构建步骤统一置于 `TC-ANIMATION-001` 的步骤 1；其余用例不在步骤中重复构建，而是通过依赖用例字段间接依赖其产出的已构建载体。

**为什么必须统一格式**：本文件由 `tools/check/check_manual_test_format.py` 解析守护（CTest 用例 `check_manual_test_format`，校验字段名与顺序、取值域、依赖拓扑、步骤-预期同号映射与执行记录表格式）。上述约定使每个字段可用**单条正则**校验，无需自然语言推断。

### 1.6 操作步骤与预期结果编号规则

**预期结果与操作步骤同号一一对应**：`预期结果 N` 即「步骤 N 的对应结果」。

1. 预期结果的序号**等于**其对应步骤的序号，两者严格相等——不得因省略而重排、压缩或重新连续编号。
2. 纯执行步骤**不产生预期结果**，其序号在预期结果列表中直接跳过。
3. 故预期结果的序号**不要求连续**，条数也无需等于步骤数。
4. 步骤列表中，纯执行步骤在文本末尾标注「（纯执行，无预期结果）」，供与预期结果列表逐号对照。

示例：某用例有步骤 1、2、3、4，其中步骤 3 为纯执行步骤，则该用例的预期结果共 3 条，编号为 1、2、4。

**该规则解决的问题**：执行者按步骤顺序推进时，编号本身即是映射关系，无需自行推断「这条预期结果对应哪一步」；执行记录可借步骤号精确落到失败点。

### 1.7 判定与记录

| 结果 | 含义 |
|:---|:---|
| PASS | 全部非空预期结果均满足 |
| FAIL | 任一非空预期结果不满足；须在记录表登记该预期结果对应的**步骤号**、实际现象与缺陷编号 |
| BLOCKED | 前置条件无法满足（如载体构建失败），未开始执行 |
| SKIP | 主动判定不适用于本环境（如被测平台与用例限定平台不符） |

输出中含**时间戳、线程 id、行号**的字段属可变部分，判定时只核对格式与是否出现，不比对具体值。

**执行记录表格式**（列顺序固定）：

| 列 | 格式 | 允许值 / 正则 |
|:---|:---|:---|
| 用例编号 | 单行 | 与 §2 用例编号一致 |
| 执行日期 | 单行 | `^\d{4}-\d{2}-\d{2}$` |
| 执行人 | 单行 | 自由文本 |
| 结果 | 单行 | `PASS` / `FAIL` / `BLOCKED` / `SKIP` |
| 失败步骤号 | 单行 | 留空（未失败或未执行），或 `^\d+(, \d+)*$`（多个以半角逗号加一个空格分隔） |
| 实际现象 | 单行 | 自由文本 |
| 缺陷编号 | 单行 | 留空或缺陷单号 |
| 备注 | 单行 | 自由文本 |

未执行的取值一律**留空单元格**，不写 `-`、`TBD`、`N/A` 等占位符。

### 1.8 覆盖取舍

本模块 4 个头的行为在人工侧的观测点**全部落在「运动观感」上**，因此其取值不表现为「哪些头不纳入」，而表现为「纳入的观测维度」。

**纳入人工测试的观测维度**：

| 维度 | 人工判定优于程序判定的原因 |
|:---|:---|
| 连续性 | 「无跳变、无闪断」是连续时间轴上的性质，逐帧断言无法覆盖 |
| 周期准确性 | 单程时长是否符合设定值，需在真实帧率下计时 |
| 裁剪正确性 | 缩放溢出是否被正确裁在舞台内，属像素级观感 |
| 多动画独立性 | 两路不同周期动画长期共存时的相互干扰，程序难以构造 |
| 长期稳定性 | 数分钟运行后的漂移与累积误差，单测运行时长无法覆盖 |
| 遮挡与最小化恢复 | 帧驱动在窗口不可见期间的行为，需真实窗口环境 |
| 确定性 | 静态展示值跨运行一致（纯函数无随机源） |

**不建议由人工覆盖的部分**：`easing.h` 的曲线数值映射、`spring.h` 的数值积分结果、`timeline.h` 的区间展开结构、`animator.h` 的控制器状态机——这些均有唯一解或确定解，程序判定更精确，已有覆盖如下：

| 头 | 已有覆盖 |
|:---|:---|
| `animator.h` | `tests/unit/utest_animator.cpp` |
| `easing.h` | `tests/unit/utest_easing.cpp` |
| `spring.h` | `tests/unit/utest_spring.cpp` |
| `timeline.h` | `tests/unit/utest_timeline.cpp` |

这些头的**观感表现**已由 TC-ANIMATION-002 至 TC-ANIMATION-004 覆盖，故不另立数值型人工用例。

## 2 用例清单

### 2.1 载体基线

#### TC-ANIMATION-001 载体构建与启动基线

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-ANIMATION-001 |
| 测试目的 | 确认载体可构建、启动并进入帧循环，且其启动特征与经 `run_demo` 启动的 demo 不同 |
| 前置条件 | 位于仓库根目录；`build/` 已完成 CMake 配置；本机工具链可用 |
| 依赖用例 | 无 |
| 操作步骤 | 1. 构建载体：`cmake --build build --target demo_animation`（纯执行，无预期结果）<br>2. 启动 `./build/demo_animation.exe 1> out.txt 2> err.txt`（纯执行，无预期结果）<br>3. 查看窗口标题与客户区尺寸<br>4. 查看 `err.txt` 与 `out.txt` 的行数<br>5. 关闭窗口，记录进程退出码 |
| 预期结果 | 3. 标题为 `Animation · Aurora Demo`，客户区约为 520×520<br>4. `err.txt` 与 `out.txt` **均为 0 行**——该载体经 `au::App()` 自行实现帧循环，不经过 `run_demo`，故不产生 `[run_demo] window shown` 提示（**不得判为缺陷**）<br>5. 退出码为 0 |

### 2.2 循环动画的观感

#### TC-ANIMATION-002 背景色往返呼吸连续无跳变

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-ANIMATION-002 |
| 测试目的 | 验证由补间驱动的颜色动画在往返循环中保持连续，且单程时长与设定值一致 |
| 前置条件 | 载体 `demo_animation` 已构建成功；可清晰观察窗口内 `color pulse` 方块 |
| 依赖用例 | TC-ANIMATION-001 |
| 操作步骤 | 1. 启动 `./build/demo_animation.exe`（纯执行，无预期结果）<br>2. 注视 `color pulse` 方块，观察其背景色的变化方式<br>3. 用秒表测量一次单向变化（从蓝变为粉红，或反向）的时长<br>4. 连续观察约 10 秒，统计跨越方向的次数 |
| 预期结果 | 2. 背景色在蓝色与粉红色之间**连续渐变**（中间经过二者之间的过渡色），无瞬间跳色、无闪烁<br>3. 单程约 1.6 秒（与该动画控制器的时长设定一致），目测误差不超过 ±0.3 秒<br>4. 10 秒内约完成 6 次单程（周期 3.2 秒）；全程无停滞、无卡在端色不动、无速率突变 |

#### TC-ANIMATION-003 缩放往返被舞台裁剪，不越界

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-ANIMATION-003 |
| 测试目的 | 验证变换（缩放）绘制超出自身布局盒时，被外层裁剪约束在舞台内，不波及其他控件 |
| 前置条件 | 载体 `demo_animation` 已构建成功；可清晰观察 `Ping-pong scaling` 区域 |
| 依赖用例 | TC-ANIMATION-001 |
| 操作步骤 | 1. 启动 `./build/demo_animation.exe`（纯执行，无预期结果）<br>2. 注视 `Ping-pong scaling` 下方的绿色方块，观察其尺寸变化范围<br>3. 在方块放大到最大时，检查其四条边与舞台边界的关系<br>4. 检查方块周边的说明文字与相邻控件是否被覆盖 |
| 预期结果 | 2. 方块在约 0.6 倍至 1.4 倍之间往返缩放，两端均可见且不消失<br>3. 放大到最大时，方块四边仍完整落在舞台边界之内，边缘平直（未被裁出缺口）<br>4. 下方与上方的说明文字、以及相邻控件**均未被遮挡或覆盖**；若方块任何部分越出舞台并覆盖了其他控件，即 FAIL |

#### TC-ANIMATION-004 两路动画相互独立

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-ANIMATION-004 |
| 测试目的 | 验证两路周期不同的动画长期共存时互不干扰（相位漂移属正常，速率与平滑度不应受影响） |
| 前置条件 | 载体 `demo_animation` 已构建成功 |
| 依赖用例 | TC-ANIMATION-001, TC-ANIMATION-002 |
| 操作步骤 | 1. 启动 `./build/demo_animation.exe`（纯执行，无预期结果）<br>2. 持续观察约 20 秒，记录两路动画的相位关系变化<br>3. 在缩放幅度处于最大与最小两种时刻，分别观察颜色呼吸的速率与平滑度 |
| 预期结果 | 2. 两路动画周期不同，相位随时间漂移并周期性重合——**此为正常表现**（周期互质级别），不判 FAIL<br>3. 颜色呼吸的速率与平滑度在缩放幅度最大、最小时**保持一致**；缩放不引起颜色动画变速、停顿或抖动 |

### 2.3 帧驱动与稳定性

#### TC-ANIMATION-005 长时间运行无漂移与逐渐卡顿

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-ANIMATION-005 |
| 测试目的 | 验证动画在持续运行数分钟后不出现单程时长漂移或性能劣化 |
| 前置条件 | 载体 `demo_animation` 已构建成功；可连续观察约 5 分钟 |
| 依赖用例 | TC-ANIMATION-001, TC-ANIMATION-002 |
| 操作步骤 | 1. 启动 `./build/demo_animation.exe`（纯执行，无预期结果）<br>2. 记录启动初期一次颜色单向变化的时长（纯执行，无预期结果）<br>3. 让窗口持续运行约 5 分钟后，再次测量一次单向变化时长（纯执行，无预期结果）<br>4. 比较两次测得值 |
| 预期结果 | 4. 两次测得值之差不大于 ±0.3 秒（无累积变快或变慢）；观察期间无逐渐加重的卡顿、无冻结、无窗口无响应 |

#### TC-ANIMATION-006 遮挡与最小化后的恢复

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-ANIMATION-006 |
| 测试目的 | 验证动画窗口在不可见期间不推进帧、恢复后能继续运行且不永久停摆 |
| 前置条件 | 载体 `demo_animation` 已构建成功；本机可开多个窗口 |
| 依赖用例 | TC-ANIMATION-001 |
| 操作步骤 | 1. 启动 `./build/demo_animation.exe`（纯执行，无预期结果）<br>2. 用另一窗口完全遮挡动画窗口约 5 秒（纯执行，无预期结果）<br>3. 移开遮挡窗口，观察动画恢复后的表现<br>4. 将动画窗口最小化约 5 秒后还原，观察其表现 |
| 预期结果 | 3. 动画继续运行；若出现一次相位跳跃（不可见期间累积的帧间隔在恢复时被一次计入），属**预期表现**，不判 FAIL<br>4. 同步骤 3；两步骤中若出现「动画永久停止不再变化」或「恢复后持续抖动、速率紊乱」，即 FAIL |

### 2.4 静态展示值的确定性

#### TC-ANIMATION-007 静态展示值跨运行一致

| 项目 | 内容 |
|:---|:---|
| 用例编号 | TC-ANIMATION-007 |
| 测试目的 | 验证窗口底部三项静态展示值由纯函数计算，不含时间或随机源，跨运行稳定 |
| 前置条件 | 载体 `demo_animation` 已构建成功；可读取窗口底部三行文本 |
| 依赖用例 | TC-ANIMATION-001 |
| 操作步骤 | 1. 启动 `./build/demo_animation.exe`（纯执行，无预期结果）<br>2. 读取窗口底部三行文本并逐字记录<br>3. 关闭窗口后重复启动共四次（纯执行，无预期结果）<br>4. 比较四次记录的差异 |
| 预期结果 | 2. 三行分别为 `curve@0.5 = ` 后接一个数值、`spring value = ` 后接一个数值、`keyframe@0.5 = rgb(r,g,b)`（r/g/b 为 0–255 的整数）<br>4. 四次运行的三行文本**完全一致**；若任一数值出现差异即 FAIL（三项均为纯函数计算，不含时间或随机来源） |

## 3 执行记录表

| 用例编号 | 执行日期 | 执行人 | 结果 | 失败步骤号 | 实际现象 | 缺陷编号 | 备注 |
|:---|:---|:---|:---|:---|:---|:---|:---|
| TC-ANIMATION-001 | | | | | | | |
| TC-ANIMATION-002 | | | | | | | |
| TC-ANIMATION-003 | | | | | | | |
| TC-ANIMATION-004 | | | | | | | |
| TC-ANIMATION-005 | | | | | | | |
| TC-ANIMATION-006 | | | | | | | |
| TC-ANIMATION-007 | | | | | | | |
